## 2026-08-14T17:51:16Z

Started implementation. Confirmed /events is non-blocking SSE but currently publishes tip-only payloads; transaction receipts already exist in the stock-compatible new_block observer payload. The delivery plan is to stream those existing payloads through /events, then join them in nano-tui without adding an archive or RPC route.

## 2026-08-14T20:52:39Z

Extended the existing event dispatcher into a sequenced local SSE feed and made /events publish the same stock-compatible new_block payload sent to observers, including explicit receipt commit state. This adds no route or receipt archive. All 41 nano-rpc tests passed (1 live-node test ignored); the six dispatcher tests passed again after the committed field was added. Strict clippy reaches an unrelated too_many_lines warning in the concurrent typed-staging chainstate change.

## 2026-08-14T21:03:39Z

Correction after full conformance: transaction receipt commit state is already represented by the stock status vocabulary (success commits; abort/failure does not). Adding a committed field to transaction objects changed stacks-core's observer schema and failed the exact 340-block payload comparison. Commit 451b195d removes that non-stock field while retaining status, raw result, ordered events and all five cost dimensions in the local SSE payload. The exact stock-shape unit test and event-observer comparison are green.

## 2026-08-17T07:44:05Z

Completed the live outcome explorer in commits 307fe8bd and bb627583. A dedicated bounded SSE reader uses the existing /events route, joins by exact block and transaction IDs, retains the same 200-block lifetime, reconnects, and marks sequence gaps or unsequenced history unavailable. The transaction view now separates execution from signed intent, decodes committed results, names aborts and VM errors, shows all five exact cost dimensions with integer current-limit shares, and renders ordered STX/FT/NFT/contract events without raw JSON. Committed-tree gates: cargo test -p nano-tui 36/36; cargo clippy -p nano-tui --all-targets -- -D warnings green; cargo fmt -p nano-tui -- --check green; live read-only /events preflight on 127.0.0.1:20492 returned HTTP 200 text/event-stream. No archive, RPC route, or server change was added.
