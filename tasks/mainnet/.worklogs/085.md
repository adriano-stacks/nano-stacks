## 2026-08-09T17:25:56Z

2026-08-09: Reconciled current source policy: ignored-tests.toml has 19 infrastructure, 11 out-of-scope, one covered and one tool entry, with zero semantic/unclassified; known-differentials.toml has zero entries. The source↔inventory release gate is green. Only execution of the named infrastructure jobs remains.

## 2026-08-11T23:54:04Z

2026-08-12: Audited `.github/release.env.example` against every required conditional and ignored infrastructure gate. The example already covered the real-mainnet 064/086/098 scratch pairs and all Hacknet/hosted-signer inputs, but omitted task111's mandatory stopped-chainstate replay. Added `NANO_111_SOURCE` and `NANO_111_SCRATCH`. The remaining referenced-but-undocumented names are workflow-owned toolchain variables, release-forced safety switches, or the explicitly optional trie diagnostic. This closes the release-bundle documentation gap only; the task remains open until the named infrastructure qualification job actually runs every required entry.

## 2026-08-11T23:54:58Z

The release-qualification workflow also omitted Task111 from its fresh reflink preparation. Added fresh_reflink 111, so the required source remains immutable and each qualification run receives a unique same-filesystem scratch that the existing trap cleans up.

## 2026-08-12T20:36:14Z

Closed two concrete release-bundle omissions found by comparing ignored-tests.toml/conditional-tests.toml with .github/release.env.example: the qualification bundle now declares NANO_052_SOURCE/SCRATCH for the exact 8,695,878 receipt gate and NANO_MAINNET_SIGNER_EVIDENCE for the cycle-141 signer-weight gates; the workflow creates a fresh reflink for task052 like the other divergence fixtures. actionlint, diff-check, and taskmd validation pass. The named infrastructure run itself remains pending.

## 2026-08-12T21:00:11Z

Release-input preflight found that `scoreboard` deliberately reports zero mainnet depth when `NANO_MAINNET_ANCHOR` is absent, while the example qualification bundle omitted it. The bundle now names the canonical imported checkpoint height 8,665,600 from the capture provenance, so the final report will measure the full executed depth rather than silently defaulting its anchor to the tip. The two cycle-141 signer gates also now resolve the same `NANO_MAINNET_STATE` working-directory contract as the scoreboard, preferring its complete `chainstate/` child. Rustfmt, diff-check and taskmd validation pass; the Rust gate and named infrastructure run remain pending behind the isolated replay.

## 2026-08-12T21:05:19Z

Closed the enforcement half of the replay-anchor preflight. A qualifying `release-report` now refuses to run its scoreboard without both the explicit `--state` and a numeric `NANO_MAINNET_ANCHOR`, and passes the CLI state path directly into both the scoreboard and conformance/infrastructure subprocesses instead of relying on a coincidentally matching inherited variable. The intentional `--no-gates` offline audit remains non-qualifying and may still print the explicit no-state row. Rustfmt, diff-check and taskmd validation pass; focused xtask tests and strict Clippy remain queued behind the isolated replay.

## 2026-08-13T21:11:14Z

2026-08-13: all three hosted-signer infrastructure tests ran green on fresh post-f787569e observer directories (3/3; 113 shared receipts exact). Full infrastructure/release-qualification inventory remains open.

## 2026-08-14T00:47:14Z

2026-08-14: The inventoried full-Hacknet infrastructure test `hacknet_keeps_working_with_a_nano_participant` ran green on fresh project `task085waterfall2` with the current release artifact: 103 canonical blocks across cycles 15->16, nano signature 103/103, 7 nano-mined blocks, all five transaction classes successful, 12 sortitions/3 miners, and waterfall weight 10/30. A participant-1-specific Hacknet API DNS assumption was isolated with disposable RPC/event replay proxies; observer evidence is retained. This executes that named infrastructure entry, not the entire release-qualification inventory, so task 085 remains open.

## 2026-08-14T01:48:07Z

2026-08-14 fresh isolated hosted-signer qualification used release artifact c50b6e700797685790ced6987d5590f99dd948da99723bc99900905c9916b1e7 under hacknet-task085hosted-20260814. The stock chain reached burn 280 / Stacks 151; after proving checkpoints anchored at 277 and exactly 280 cannot seed the first waterfall without captured payout state, the successful authenticated checkpoint anchored at burn 284 (Stacks 163, source 870d3fe5..., root a3eb6ace..., two authenticated blocks). Participant 3 was selected while participant 2 held the latest sortition. Nano served cycle 15 before handoff and advanced above hosted-start height 196. The exact hosted_signer infrastructure filter passed 3/3 in 22.56s: Accepted(NotRejected) block cc24b3c8..., admitted and mined tx d7dc4972..., 41 new_block / 3 new_burn_block / 116 StackerDB events, and all 39 overlapping stock/nano receipts exact. Full release inventory/qualification remains open.

## 2026-08-14T11:10:44Z

Fresh task122n infrastructure rerun exposed and fixed a harness-only false miner requirement: hosted-signer mode intentionally has no [miner], but verify exported NANO_MINER_PUBLIC_KEY whenever a stale key file existed. The corrected gate explicitly unsets the key unless the running config enables mining and suppresses Cargo's secret-bearing command echo. Exact two-cycle gate green: 310 canonical blocks across cycles 44..46; every block carried the replaced signer; 260 transfers, 6 deploys, 546 calls, 25 tenure changes, 25 coinbases; 26 sortitions across 2 miners; cycle 46 waterfall signer weight 4/10; 1/1 in 1494.09s.

## 2026-08-14T13:46:52Z

Qualification repair on 2026-08-14: the first required 297-test run executed 284 tests successfully and exposed four real failures. The burn-spend and PoX-boundary seed context are fixed. A new same-chain 340-block capture (Stacks 1880..2219) ends on three genuine pox-5 locks; validate-fixtures passes, and both an isolated in-tree preflight and the installed-tree default conformance suite pass 288/288 with 9 infrastructure ignores. The live mainnet P2P inventory failure remains unresolved, so the required infrastructure qualification is not yet green.

## 2026-08-14T14:04:29Z

2026-08-14: The sole failure from the first full infrastructure run is repaired and independently green: GetNakamotoInv now uses stacks-core's modulo-one wire boundary after the waterfall. Four public peers answered the corrected cycle hash, all four claimed tenures, and the schedule fetched 223 blocks from two peers. The complete required qualification must still be rerun before closing this task.

## 2026-08-15T00:27:11Z

Post-fix current-main observer qualification on fresh task122q streams: release node bf35d7cf produced three successful contract deployments at Stacks heights 1905, 1927 and 1943; nano and stock matched status=success, raw_result=0x0703 and all five execution-cost dimensions for each. The exact ignored observer gate then compared 84/84 shared new_block receipts and passed 1/1, with 14 new_burn_block and 771 stackerdb_chunks payloads present. This closes the sole failure found by the first combined infrastructure attempt; the tasks remain open until a fresh complete 22-test run is green.
