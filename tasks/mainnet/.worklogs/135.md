## 2026-08-17T03:36:15Z

Started from clean main at 80eccf52. Audit: the sole Nix release package builds nano-node/stacks-node, whose package-level dependency graph includes nano-miner, nano-signer, nano-stackerdb, nano-mempool and the broad nano-rpc server; runtime::run starts one combined role supervisor and Config exposes miner/signer/proposal/RPC switches. This fails the task structurally even when those fields are unset. First slice will define an executable capability/forbidden-dependency/route matrix and a separate follower package boundary; subsequent slices will move the follower loop behind that boundary rather than claim configuration-disabled code is absent.

## 2026-08-17T03:39:17Z

First atomic slice defines the closed follower boundary in release/follower-policy.json and docs/follower-artifact.md. The future release artifact is package nano-follower/binary stacks-follower; its six in-process capabilities map to an explicit internal-crate allowlist, health/metrics are loopback-only, the public HTTP surface is empty, and miner/signer/mempool/StackerDB/TUI/oracle/broad RPC/current nano-node packages plus mutation-capable config/routes are forbidden. Inbound P2P serving and persistent native modules are explicit measurement decisions rather than assumed requirements. A conformance regression parses and enforces the closed matrix. Evidence: exact focused test green, strict conformance-test Clippy, jq, rustfmt, diff-check and taskmd validation green. This checks only the matrix task; the current stacks-node artifact still violates it and remains the next implementation boundary.

## 2026-08-17T03:54:18Z

Second atomic slice creates the `nano-follower` library boundary and moves the existing sortition tracker and authenticated staging store into it without rewriting either implementation. `nano-node` re-exports those modules, so its callers and behavior remain unchanged. The first dependency gate correctly rejected `nano-follower -> nano-sync -> nano-mempool`; `nano-sync` now keeps its mempool/account client behind a default-on `mempool` feature, while the follower disables that feature. Existing node/miner consumers retain the default surface, and the follower graph has no forbidden internal package. Evidence: the exact dependency-tree policy gate passed; `nano-sync` passed 27 runnable tests with 6 infrastructure tests ignored; `nano-follower` passed all 32 extracted tests with default features disabled; `nano-node --lib` passed all 69 tests; strict all-target Clippy passed both the isolated follower graph and the default sync/node/conformance graph; rustfmt and diff-check passed. This establishes the reusable follower core only. The separate `stacks-follower` binary and packaged artifact remain deliberately unchecked.

## 2026-08-17T04:01:58Z

Third atomic slice relocates the checkpoint trust/adoption API into `nano-follower::checkpoint` and leaves `nano-node` source-compatible through re-exports. The moved code still binds the manifest height, state identifier and root to the threshold-signed Nakamoto header and persists the same provenance/bundle receipt; no execution behavior changed. Evidence: both packages pass all-target check, all 17 checkpoint-filtered node regressions pass, the follower dependency policy remains green, and strict all-target Clippy, rustfmt and diff-check pass. The next boundary is the executor itself; no binary or artifact checkbox is claimed by this relocation.

## 2026-08-17T04:06:58Z

Fourth atomic slice moves the bounded executed-block archive into `nano-follower`. Its storage, restart, tenure, ambiguity, pruning and retraction behavior is now available without the compatibility server; a `node-rpc` feature contains only the legacy `ExecutedBlocks` trait adapter used by `nano-node`. The default follower dependency graph therefore remains free of `nano-rpc`. Evidence: the default follower passed all 37 tests, including the five archive regressions; the node adapter compiled; isolated follower and node-feature strict Clippy passed; and the exact follower dependency policy gate, rustfmt and diff-check are green. This is another executor prerequisite, not the standalone binary or packaged artifact.

## 2026-08-17T04:17:20Z

Fifth atomic slice moves the single `CheckpointExecutor` implementation and its catch-up, local-sortition, fork, header-backfill and checkpoint-continuation logic into `nano-follower`; `nano-node` is now a compatibility module shell re-exporting that implementation. Node-only event/RPC, mempool and miner adapters are behind the non-default `node-adapters` feature, while archive ownership remains in the default follower. Narrow executor methods replace cross-crate access to raw `ChainState`; there is still only one execution implementation. The default normal dependency graph is exactly the 15 allowed internal packages and excludes every forbidden role package. Evidence: default follower passed all 40 tests; adapter-enabled follower passed all 42 tests; node passed all 59 remaining role/runtime tests; both configurations pass all-target check and strict Clippy; and the exact dependency policy, rustfmt and diff-check are green. This completes the reusable execution core, but not yet the standalone loop/binary, so the binary/package checkbox remains open.

## 2026-08-17T04:26:08Z

Sixth atomic slice moves checkpoint bundle construction/verification and append-only builder signature policy into `nano-follower`, so the standalone follower can authenticate startup material without linking `nano-node`. Production ownership is in the default follower graph; a narrow `test-support` feature exposes only deterministic checkpoint fixtures needed by legacy node runtime tests. Evidence: combined all-target check green; default follower 49/49 and node 50/50 tests green; exact follower dependency policy green with the same 15-package allowlist; strict all-target Clippy green for default follower and for follower node-adapters/test-support plus nano-node; rustfmt and diff-check green. This is trust/runtime composition work only: no standalone binary or package checkbox is claimed.

## 2026-08-17T04:36:13Z

Seventh atomic slice adds the strict follower-only configuration contract in nano-follower. Its only top-level tables are follower, burnchain and checkpoint; deny_unknown_fields makes miner, signer, proposal, inbound P2P, public or mutating RPC, observer and compatibility switches unrepresentable. Mainnet defaults to outbound published P2P seeds, requires independently signed checkpoint material, and health plus metrics are always loopback. Evidence: 5 focused parser/default/refusal tests and all 54 follower tests green; exact dependency policy remains green; strict all-target Clippy, rustfmt and diff-check green. This commits configuration truth before the executable loop and claims no binary gate yet.

## 2026-08-17T04:41:14Z

Eighth atomic slice implements the follower own bounded loopback observation server without nano-rpc. It serves only GET /health and GET /metrics on the separately validated loopback binds, reports readiness, execution position, state root and P2P counts, bounds request parsing to 1024 bytes, and returns 404 for compatibility or cross-surface routes. Evidence: 2 focused route/readiness tests and all 56 follower tests green; strict all-target Clippy, exact dependency policy, rustfmt and diff-check green. The runtime will publish into this surface; no standalone binary claim is made yet.

## 2026-08-17T04:44:13Z

Ninth atomic slice gives nano-follower its own read-only Bitcoin transport boundary and execution-context construction. The same BurnchainSource dispatches configured Bitcoin Core RPC or Esplora bytes behind the BitcoinSource trait, and configured PoX-5 activation is applied before execution. Evidence: 2 focused transport/context tests and all 58 follower tests green; strict all-target Clippy, exact dependency policy, rustfmt and diff-check green. This is startup composition only and does not claim the binary yet.

## 2026-08-17T04:49:24Z

Tenth atomic slice extracts signed checkpoint adoption into nano-follower. Before chainstate opens it independently verifies the local Bitcoin header, external builder threshold, bundle content and path confinement, signer attestation and profile; persisted provenance keeps only the bounded content-root receipt and still reauthenticates external signatures and Bitcoin canonicality on restart. Evidence: focused wrong-view, escaped-path, accepted-adoption, payload-free restart and changed-view refusals green; all 60 follower tests, strict all-target Clippy, exact dependency policy, rustfmt and diff-check green. The remaining startup chainstate/open loop will consume this module before the standalone binary is claimed.

## 2026-08-17T04:59:25Z

Eleventh atomic slice moves authenticated chainstate startup into nano-follower. Fresh state now adopts and opens the verified checkpoint, seeds only checkpoint accounting, derives and authenticates the bounded block suffix and boundary winner from the local Bitcoin/sortition history, and applies the anchor. Restart instead selects the deepest sealed state with a committed ledger, bounds peer ancestry lookup, discards unreferenced MARF residue, restores the exact ledger and refuses incomplete reward history. Evidence: all 64 follower tests green, including fixed-width boundary, ledger selection, ledger refusal and maturity-window regressions; both closed capability/dependency policy tests green; strict all-target follower Clippy, rustfmt and diff-check green. This supplies the startup constructor the standalone loop will call and still does not claim the binary/package checkbox.

## 2026-08-17T05:04:51Z

Twelfth atomic slice implements the standalone follower's outbound-only P2P transport. It creates a persistent OS-random identity and peer table, seeds and maintains only outbound authenticated sessions, walks neighbors and exchanges inventory, publishes discovered HTTP acquisition endpoints, derives every advertised Bitcoin view from the configured local source, and advertises no service or data URL. There is no listener or inbound service construction in this graph. Evidence: the persistent quiet-identity regression and all 65 follower tests green; both closed dependency/capability policy tests green; strict all-target follower Clippy, rustfmt and diff-check green. The execution loop will consume this discovery handle next; the binary/package checkbox remains open.

## 2026-08-17T05:12:27Z

Thirteenth atomic slice composes the standalone follower runtime. One exclusive state owner starts outbound discovery and loopback observation, resolves a reachable network peer, authenticates fresh or resumed chainstate, chooses canonical candidates against the follower's locally derived burn view and locally recorded signer weights, spreads bounded history acquisition across the discovered pool, executes through the single CheckpointExecutor, persists staging and executed blocks, and advances Bitcoin/sortition state even while Stacks is idle. SIGINT/SIGTERM finish the current bounded round before releasing the lock. Evidence: the exclusive-owner regression and all 66 follower tests green; both closed dependency/capability policy tests green; strict all-target follower Clippy, rustfmt and diff-check green. This is the executable runtime library; the next slice adds and packages the `stacks-follower` entrypoint before any binary checkbox is claimed.

## 2026-08-17T05:19:35Z

Fourteenth atomic slice adds the explicit `stacks-follower` binary and makes it the flake's default package/container while retaining `stacks-node` only as a separately named development artifact. The CLI exposes exactly start, check-config, config-schema, build-identity and compatibility-profile; it has no role, public-service or environment capability switch. The package carries its own SBOM, resolved dependency tree, closed policy, generated schema/profile/identity and hardened service unit. Evidence before checkpoint: 66 library plus 1 binary test green, including an exact forbidden-command/help inventory; both dependency/capability policy tests green; strict all-target follower Clippy, Nix parse, rustfmt and diff-check green. The clean committed-flake Nix build and installed-artifact inspection are intentionally the next gate; the failed path-flake attempt was stopped because it copied the ignored target directory, and no package checkbox is claimed yet.

## 2026-08-17T05:28:23Z

Fifteenth atomic slice verifies the committed flake artifact itself. A clean nix build of .#stacks-follower produced /nix/store/ir4rhzwr83idg90hf7nl3zzq6mpwhsjg-nano-stacks-follower-0.1.0-7a4e82d10648 with a 90.0 MiB closure and 36,109,416-byte executable. The installed CLI exposes only start, check-config, config-schema, build-identity and compatibility-profile; the schema has exactly burnchain/checkpoint/follower; build identity names source 7a4e82d10648a8bd3372a777e2d02036acb0b42c and profile stacks-mainnet-epoch-4.0-v1; the packaged dependency inventory contains none of nano-node, miner, signer, mempool, rpc, stackerdb, tui, oracle or conformance; and the package includes immutable identity/profile/schema/policy/SBOM/dependency/service evidence. The flake's default package pname is nano-stacks-follower. This proves the separate artifact and its outbound-only/loopback closed configuration; mandatory release inspection, packaged execution/restart and measured liveness tasks remain open.

## 2026-08-17T05:48:46Z

Sixteenth atomic slice makes the closed package boundary a mandatory executable gate. stacks-follower now emits a surface inventory tied directly to the two observation route constants; the package finalizer runs scripts/check-follower-artifact.sh over the resolved Cargo tree, schema, policy, surface and demangled ELF symbol table and installs the hashed result; the required CI workflow independently rebuilds and reruns the inspection, comparing it to the installed record. The clean commit 5e5c0b54cec771044c7e99f02da8011b3ddda81a built /nix/store/95a4qivkwvnlbd5nq4h81zd6impw2gqy-nano-stacks-follower-0.1.0-5e5c0b54cec7. Its 90.0 MiB inspection records binary sha256 b13e2e0253d2f57da64216b871efe5c2c8ab38347f7f482c23e70b027904c46f, exactly 15 policy-allowed internal packages, /health and /metrics only, no public route, zero forbidden role/reference-engine symbol matches and the required clarity-wasm execution symbol. Follower 66 library + 2 binary tests, the exact conformance policy test, strict follower/conformance all-target Clippy, shellcheck, actionlint, formatting, diff-check and taskmd validation were green. This checks only the dependency/route/binary inspection task; packaged behavioral replay and measurements remain open.

## 2026-08-17T06:36:22Z

Seventeenth atomic slice adds the mandatory packaged lifecycle gate and fixes the production recovery bug it exposed. The exact Nix artifact imports the independently authenticated checkpoint, discovers its HTTP source through outbound P2P with no configured HTTP peers, follows a moving tip, persists and restarts, retracts to a common ancestor for a longer forged branch, refuses that branch on local miner/signature authentication, prunes the rejected block and every staged descendant while preserving unrelated siblings, then restarts against an honest peer and reaches the full tip with every captured state root verified. The rejected-branch primitive is commit 792135c8 and the package/CI gate is ffde0bf9. Clean `nix develop --command scripts/ci.sh follower-artifact` built `/nix/store/2yc34xcpnrgwrqr8mi08nqckr47gvkbf-nano-stacks-follower-0.1.0-ffde0bf9547b`, independently matched its installed dependency/route/symbol inspection, and passed the exact lifecycle test in 39.53s. The focused sibling-preservation regression, strict all-target Clippy for nano-follower+nano-conformance, six release-inventory tests, shellcheck, actionlint, taskmd validation and diff-check are green. This checks the packaged checkpoint/catch-up/fork/restart/tip task only; separate optional-role authority, measured native-module/inbound-service decisions, and full receipt-equivalence evidence remain open.

## 2026-08-17T07:02:00Z

Eighteenth atomic slice removes persistent native modules from the follower's compile-time dependency graph while retaining them in the development node. The follower, p2p and sync crates disable nano-chainstate's default persistent-module feature; nano-vm's no-default implementation uses only its in-memory module cache; the closed policy now forbids nano-wasm-cache. The packaged regression checks after every follower process exits that no native-module directory exists. Clean commit dec00fe7 built `/nix/store/slcki430rsx8dzp3pnsvym9nhd53ikdi-nano-stacks-follower-0.1.0-dec00fe78490`, whose 94,278,296-byte closure and 36,047,880-byte executable exclude nano-wasm-cache and all forbidden roles. The same outbound-only 12-block lifecycle completed in 39.71s, versus 39.53s for the persistent-module baseline; both are below the 60-second condition bound. The cache-free run had zero configured HTTP peers, observed an outbound P2P session, discovered the HTTP source through that session, passed checkpoint/catch-up/fork/restart/root checks, and left zero persistent module directories after follower exits. `release/follower-liveness.json` binds the revisions, artifact hashes, sizes, timings and decisions; the focused conformance check makes that policy pointer and bound mandatory. The measurement therefore retains neither inbound serving nor persistent native modules. Optional-process write isolation and complete full-node receipt equivalence remain open, so Task 135 stays in progress.
