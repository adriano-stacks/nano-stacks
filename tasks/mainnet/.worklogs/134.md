## 2026-08-17T02:05:15Z

Started from clean main at 372ed083. Source audit: checkpoint.toml currently carries format, Stacks height/state ID/root, one Bitcoin height and profile fingerprint; it hashes no payload file or chunk and cannot reject substitution, truncation or extra files as one bundle. Startup independently checks the attesting Nakamoto header and reward-set threshold plus the active compatibility profile, and interrupted MARF import is fail-closed, but configuration names each path separately and carries no content manifest, Bitcoin-view hash, builder policy or builder signature. release-candidate signs only the small checkpoint.toml. First atomic slice is a deterministic versioned bundle manifest with sorted file/chunk SHA-256 inventory and an offline verifier that refuses omitted, changed, extra and non-regular files before import.

## 2026-08-17T02:11:04Z

First implementation slice: nano-marf now defines nano-stacks/checkpoint-bundle/v1. The unsigned manifest duplicates and cross-checks the checkpoint format/height/state ID/root/Bitcoin height/profile, additionally binds Bitcoin block hash, attesting block/weight/threshold, Epoch40 and compiler identity, and inventories every regular payload path with exact size, whole-file SHA-256 and fixed 4 MiB chunk SHA-256. One domain-separated content root covers all claims and file/chunk digests in canonical order. Paths are sorted/portable; symlinks and special files are refused; an existing manifest is never replaced; verification rejects missing, extra, changed and truncated payloads. Two independently populated directories produce byte-identical manifests. Evidence: nano-marf 19/19 and strict all-target Clippy green. Builder signatures, local Bitcoin header proof, startup enforcement and independent external build/signature publication remain open.

## 2026-08-17T02:19:25Z

Second atomic slice wires the content manifest into the production binary as offline build-checkpoint-manifest and verify-checkpoint commands. Both paths decode block.bin, parse the bundled reward set, recompute the signer threshold with the existing checkpoint attestation, and bind the active Epoch40 compiler/profile identity; verification refuses a self-consistent forged signer-weight claim, and a below-threshold block creates no manifest. The runtime now shares the same reward-set parser. Evidence: focused bundle tests 3/3, existing reward parser regression 1/1, full nano-node 93/93, and cargo clippy -p nano-node --all-targets -- -D warnings green. The Bitcoin hash remains an explicit claimed input until the next local header-chain proof slice; builder signatures and import/startup enforcement remain open.

## 2026-08-17T02:26:23Z

Third atomic slice adds operator-pinned independent builder signatures. A v1 policy requires a distinct-builder threshold, canonical compressed keys, non-overlapping height-bounded rotations and explicit revocation heights; duplicate keys and impossible thresholds are refused. Each builder signs the domain-separated bundle content root into one create_new immutable file outside the payload directory. The verifier authenticates only locally pinned active keys and rejects self-declared attacker keys, insufficient signatures, changed signature bytes and noncanonical entries. stacks-node now has sign-checkpoint-manifest and signed verify-checkpoint commands. Evidence: focused signature policy 4/4, full nano-node 97/97, cargo clippy -p nano-node --all-targets -- -D warnings, taskmd validation and diff-check green. This implements the policy and offline mechanism; real independent builder publication/evidence remains open.

## 2026-08-17T02:32:03Z

Fourth atomic slice removes the caller-supplied Bitcoin hash trust path. Bundle build, builder signing and signed verification now all require block_hash_at(checkpoint.first_bitcoin_height) from the operator's Bitcoin Core RPC; the CLI reads its password from a file and no longer accepts a raw hash flag. The locally returned exact-height hash is bound into the manifest, and verification refuses a byte-valid, correctly signed bundle when the operator's canonical Bitcoin view differs. The bundled Nakamoto block/reward set threshold and active Epoch40 compiler/profile are still recomputed in the same pre-import path. Evidence: checkpoint-focused 15/15, full nano-node 98/98, strict Clippy and diff-check green. The first two Task 134 implementation bullets are now checked; startup/import enforcement and real external builder/rebuild evidence remain open.

## 2026-08-17T02:41:17Z

Fifth atomic slice makes signed bundle evidence mandatory for mainnet configuration and verifies it at adopt() before ChainState::open_from_checkpoint can create/import production databases. bundle, builder_policy and builder_signatures must be complete; REST is refused for this trust decision; core marf/block/reward paths must be the verified fixed files; anchor/accounting/sortition/authentication-history must resolve inside the verified bundle; policy/signatures must resolve outside it so trust cannot be self-declared. A focused startup test proves wrong Bitcoin view and an escaped anchor refuse while the target state directory remains absent, then the valid two-builder package records provenance. Evidence: config 11/11, focused pre-import 1/1, full nano-node 100/100, strict Clippy and diff-check green. Full-payload verification currently repeats on restart; the next slice will persist the signed content-root receipt so restart cost remains bounded.

## 2026-08-17T02:58:48Z

Sixth atomic slice persists the authenticated bundle content root, exact Bitcoin height/hash and original threshold builder names in immutable, fsynced checkpoint provenance before import. Restarts recheck that root against the current operator-pinned policy/signatures and local Bitcoin Core view without rereading the imported MARF; the VM likewise resumes durable mainnet state without reopening discarded checkpoint bytes. Focused tests prove payload-free restart plus Bitcoin/signature refusal, provenance round-trip/immutability and the lower VM boundary. Evidence: nano-marf 20/20, nano-vm 50/50 with one intentional search ignored, nano-node 100/100, both conformance compatibility checks, strict all-target Clippy over nano-marf/nano-vm/nano-node/nano-conformance, taskmd validation and diff-check all green. The ready-to-import/offline-verifier task is checked; real independent failure-domain builds/signatures, scheduled rebuild evidence and operating documentation remain open.

## 2026-08-17T03:08:24Z

Seventh atomic slice adds an explicit CI checkpoint-reproducibility gate. Checked-in published inputs combine the real mainnet checkpoint block and cycle-140 reward set with a tiny stand-in MARF/sortition payload and a clearly synthetic fixed Bitcoin hash. The gate assembles those inputs in two independent directories, runs the production signer-proof manifest builder and verifier, requires byte equality between both builds, then requires equality with the checked-in released sample manifest. Any compiler/profile, payload or serialization change now needs an explicit manifest update. Evidence: the exact dev test and scripts/ci.sh checkpoint-sample under the real ci profile are green; nano-node 101/101, strict all-target nano-node Clippy, actionlint/shellcheck, repository formatting, diff-check and taskmd validation are green. The sample-rebuild task is checked; real failure-domain builder evidence and procedures remain open.

## 2026-08-17T03:14:04Z

Eighth atomic slice replaces the stale attestation-only operator guidance with the signed content-root procedure implemented by the node. The trust guide now covers every-byte authentication, independent builder policy, local Bitcoin Core, exact build/sign/verify commands, immutable provenance, key rotation/revocation, retention, interrupted-import recovery and new/incrementally transported checkpoint rules. The mainnet run/build guides and example config require bundle, external policy/signatures and local Core; a valid example policy is checked in. Executable documentation regressions parse both examples and compare every persisted provenance key, including bundle receipt fields, with the guide. Evidence: stacks-node check-config green, release_dependencies 8/8, strict all-target conformance Clippy, repository formatting, diff-check and taskmd validation green. The documentation task is checked. Real independent archive/node builds, published threshold signatures and a fresh operator import/follow remain open evidence.

## 2026-08-17T03:33:31Z

Ninth atomic slice binds checkpoint trust into signed release evidence. release-candidate prepare now runs the exact candidate stacks-node offline verifier against local Bitcoin Core, packages its argv/stdout/artifact hash, and copies the content-addressed bundle manifest, external builder policy, append-only signature set and immutable imported-state provenance. Prepared-candidate verification recomputes the manifest root, requires at least two authenticated builders, cross-checks checkpoint/profile/attestation/Bitcoin view/builder names/provenance, and refuses a signed false transcript; policy/signatures resolving inside the bundle are refused. In-toto dependencies and the final qualification report name the evidence hashes/root/builders, and qualification freezes the same external inputs before and after gates. Evidence: nano-marf 20/20; release_candidate 10/10 including re-signed false-transcript and insufficient-builder refusals; all 37 xtask unit tests green (CLI integration tests remain intentionally clean-tree gated until this commit); strict all-target nano-marf/xtask Clippy, actionlint, diff-check and taskmd validation green. The two distinct-failure-domain build/publication tasks remain externally open.
