# Task 146 worklog

## 2026-08-21T21:50:00Z — root cause one: charge style frozen at deployment epoch

Built the offline reproducer first: `xtask cost-both-tx` re-runs one sealed
transaction against its parent trie (the MARF keeps every ancestor) with the
consensus cost tracker through both engines; `xtask cost-both` does the same
for a hand-written call, and `xtask cost-both-batch` audits thousands of calls
over one state open. On a reflink scratch of the witness state, the sealed
receipt reproduced exactly (compiler = the inflated 165496/30/77622/4/901,
interpreter = the canonical 161448/30/76653/4/341), so the whole differential
sits between clar2wasm's generated charging and the reference, offline, in
seconds.

A charge-stream trace (temporary `NANO_TRACE_COSTS` instrumentation in a
patched clarity worktree, never committed) plus decomposition through the
callee graph localized two generator defects:

1. **Charge style keyed to the wrong epoch.** Fourteen sites in
   `words/{maps,data_vars,equal,index_of,tuples}.rs` picked the pre/post-2.05
   charging rule from `contract_analysis.epoch` — the deployment epoch — so a
   contract deployed before 2.05 (marketplace-v4 is from 2021) kept charging
   2.0's *static type sizes* for map and variable operations forever, while
   the interpreter picks the rule from the runtime epoch and charges
   serialized bytes. Static tuple-of-principal sizes dwarf serialized ones
   (the on-sale fetch charged 683 against the canonical 246), which is the
   +969 read_length / +560 write_length / most of the +4048 runtime. Five
   more inverted (`< Epoch2_05`) sites in the same files plus `hashing.rs`
   double-charged once the first fix landed. All nineteen now go through
   `WasmGenerator::charges_serialized_sizes()`, which delegates to
   `executing_epoch()` — the cost epoch `compile_for_cost_epoch` already
   plumbs — exactly like the `uses_arg_size_for_cost` sites the earlier cost
   work fixed in `tokens.rs`.

2. **Trait references sized as their type, and paying a copy they never
   make.** A trait argument's charge size is its *value*: a bare contract
   principal (148) in a Clarity 1 contract, a callable carrying its trait
   identifier (276) from Clarity 2 on. `runtime_size`'s catch-all charged the
   *type* size (276) regardless, and `visit_atom` charged
   `LookupVariableSize` on trait reads that the reference hands back owned
   from the callable context (Clarity 1 only) without any copy charge.
   Both are now version-exact (`is_trait_reference` /
   `reads_owned_callable`).

With both fixes the block-8,808,752 transaction charges **exactly the
canonical record on all five dimensions**, and the minimal shapes are pinned
as `recorded_semantics_charges` crosscheck-cost tests in `words/maps.rs`,
using `crosscheck_cost_recorded_semantics` (deploy under Epoch20/Clarity1,
charge at the current epoch) — the harness combination none of the existing
cost tests exercised, which is why the suite was green over a 30% mainnet
differential.

## Scope, measured

The hold-window sweep (`/home/aldur/mainnet-hold/cost-mismatches.jsonl`, in
flight) is finding ~30% of blocks with at least one cost mismatch. Re-running
every transaction of every mismatched block through both engines with the fix
(`cost-both-batch`) shows the three-dimension family closed and a residual,
smaller census that is almost entirely runtime-only: repeated deltas of
+8/+33/+62/+82/+1280/+1281/+24627 and a few negatives (−276), plus exactly one
operational differential — `BNS-V2::name-claim-fast` where the compiled
engine performs one extra charged read *and* write (18/7 vs canonical 17/6,
runtime −3761, identical lengths and root). The canonical record agrees with
the interpreter on every case checked. These residuals are the remaining
work; same loop: align, reduce, pin, fix.

## 2026-08-22T05:40:00Z — the three-dimension family is closed; the residual is runtime-only

The sweep finished: 1,736 of the hold window's 6,243 blocks (27.8%) carried at
least one cost mismatch against the canonical record. Re-running every
transaction of every one of those blocks through both engines with the fix
(`cost-both-batch`, 3,659 contract calls, no errors) leaves **1,611
disagreements, 1,609 of them runtime-only** — every read_length and
write_length difference is gone, on every call in the window, and both
count-bearing dimensions agree everywhere except two transactions.

So the family the differential opened with is closed. What remains is a
narrower one: runtime deltas clustered on a few repeated magnitudes (+62 on
438 calls, +33 on 312, +8 on 187, +1281/+1289 on 57, −276 on 15, and a long
tail), plus exactly two calls where nano performs one extra charged read and
write — `BNS-V2::name-claim-fast` (18/7 against the canonical 17/6, runtime
−3761, lengths and root identical), which is an operational difference rather
than a pricing one and needs its own reduction.

Localizing runtime deltas needed a new instrument. Wasm charges accumulate in
module globals and reach the tracker only at sparse flush points, so a
several-dozen-unit difference inside a million-unit transaction was smeared
across hundreds of charges. `NANO_TRACE_CHARGES` (committed) makes every
generated charge report its word label and scaling input, so the two engines'
charge streams can be aligned event by event. The first case it resolved is
the +33 family: on a `(is-eq (some burn-hash) (get-burn-header burn-height))`
the compiled engine charges one `LookupVariableSize` on a 16-byte argument
that the reference never charges — the reference's cost for reading a binding
depends on whether its consumer materializes the value or borrows it, which
the compiler currently does not model.

Two notes for whoever picks this up. The compiler identity hashes the whole
vendored tree, so *any* edit there — including a comment — moves the profile
fingerprint and invalidates a state's recorded profile; the published
checkpoint sample is repinned in the same commit as each compiler change, and
diagnostic runs belong on a reflinked scratch state. And the fixes so far
already supersede the frozen release artifact 16e0928a: its compiler is the
one with the inflated charges, so the 24-hour hold evidence gathered on it
describes an artifact that will not be the release candidate.

## 2026-08-22T12:20:00Z — second census: the +33 family closed, +62 and +8 remain

Re-ran every transaction of every mismatched hold block against the fixed
compiler: **3,658 calls compared, 1,313 disagree, down from 1,611**, and still
zero length-dimension differences anywhere. The drop is exactly the family the
borrow fix closed: `+33` fell from 312 calls to 15.

What remains, in order: `+62` on 438 calls, `+8` on 187, `+1281`/`+1289` on 57,
then a tail including negatives (`-276`, `-184`, `-92`), plus the same two
calls with an extra charged read and write (`BNS-V2::name-claim-fast`).

Three things worth knowing before picking this up.

The `+62` family is one shape and one deployer: tiny router contracts
(`SP2H674PRTZV6YW56K0FMR7GDGZE4ZC5HMYZ3CDEV.psis` and its siblings `gent`,
`phis`, `daud`, `tuna`) that forward a `(buff 400)` parameter through a trait
dispatch into `SP2H674PRTZV6YW56K0FMR7GDGZE4ZC5HMYZ3CDEV.swag`, a 32 KB
contract of trait-typed list entry points. Reducing the router alone does *not*
reproduce it — a probe of exactly that shape charges identically — so the
divergence is inside `swag`, and the next step is bisecting `swag`'s entry
points rather than the router.

Charge-stream diffing does not work on this contract, and it is worth saying
why so nobody repeats it: over the psis transaction the compiled engine makes
about 1.5x as many charge events as the reference (17,144 `get` against 11,860,
4,120 `if` against 2,683) while the two totals agree to within 62 units. The
engines decompose the same work into different numbers of events, so event
counts and per-word input multisets are both noise here; only aligned small
streams are informative, which is how `+33` was found.

`+62` cannot be a single `LookupVariableSize` — that charge is `2n+1`, always
odd — so unlike `+33` it is at least two charges or a different cost function,
which is consistent with it living deeper in a call chain.

Also recorded: the sample repin trap. The compiler identity hashes the vendored
tree *as it is on disk*, so measuring the active fingerprint in a working tree
that carries another session's uncommitted clar2wasm edit pins a value that was
never committed. That happened here and the gate caught it in a clean checkout;
the fix is to regenerate the sample in a clean worktree, which 2011cdeb does.

## 2026-08-22T14:10:00Z — the runtime residual cannot be adjudicated by the interpreter

Localizing `+62` exactly is what exposed the framing error in everything above,
so this entry is a correction as much as a result.

`+62` was localized precisely. Calling `swag.ss` on real mainnet state charges
identically to the interpreter for one and two list elements and diverges by
exactly 62 from three on; per element, `swag.sr` is exact on the slices that
dispatch to `token-stx-v-1-2` and diverges by 62 on the two that dispatch to
`token-stx-v-1-1`, which are also the ones whose execution aborts. Both token
contracts have identical `transfer` signatures, both declare the same
(different) trait, and calling either directly is exact — so the divergence
needs the dynamic dispatch *and* the failing callee together. Reduced probes of
each ingredient separately (fold over a trait list, a failing dispatch, an
arithmetic underflow, a router forwarding a buffer) all charge identically, so
the reduction is not finished.

Then the check that matters. For that transaction nano's **sealed** cost was
`runtime 57,123,371` against a canonical `57,123,733` — nano was **362 below
the real chain**, while the compiler is 62 *above* the interpreter, which puts
the interpreter roughly 424 below canonical here. **The interpreter is the
outlier on this shape, not the compiler.** Every "disagreement" counted in the
two censuses above is compiler-versus-interpreter, so those 1,313 numbers do
not say that nano disagrees with the chain; on this shape the engine they
measure against is the one further from it.

What is actually established, and against the authoritative oracle:

- Block 8,808,752 carries exactly one transaction, so its block cost *is* that
  transaction's cost and an exact canonical comparison is possible. With the
  fixed compiler it matches the canonical record on all five dimensions. That is
  the differential this task opened on, closed against the chain rather than
  against the interpreter.
- The three charging fixes were verified against the reference's own source —
  `eval` then `as_ref`, never `clone_with_cost` — not merely against the
  interpreter's behaviour.
- Every `read_length` and `write_length` difference in the window is gone.

What remains, restated honestly: the runtime dimension. Nano's sealed runtime
differs from canonical in *both* directions across the window (`-362` here,
positives elsewhere), and neither the interpreter comparison nor a single-block
canonical check can settle it at scale. Settling it needs the hold window's
blocks re-executed with the fixed compiler and their receipts compared to the
canonical record — which is this task's last subtask and exactly what unblocks
106. `cost-both-tx` cannot substitute: it runs one call against a parent without
the block's transaction prefix, so its numbers are not comparable to a sealed
receipt.

The instrument for that work now exists and is worth keeping: the charge probe
reports the runtime meter after every charge (index `-1`), so a compiled charge
stream can be compared to the reference's cost for cost rather than by event
name. That is what showed the local sequence differences around `len` and
`mod` to be ordering, not pricing.

## 2026-08-22T15:30:00Z — the canonical audit needs a re-attested checkpoint first

Tried to stand up the canonical-oracle measurement the entry above asks for: the
fixed node following mainnet, every executed block's receipts compared against
the chain's own record. It does not start, for a reason that is itself the
finding.

A node refuses to run against a checkpoint bundle whose recorded compiler
identity is not its own — `checkpoint profile 5561d364… does not match this
artifact's …` — and `[checkpoint]` is not optional even when the state is
already imported. The bundle binds the compiler that produced its state root,
and the builders signed that binding. So the cost fixes do not merely supersede
the frozen artifact 16e0928a as a *binary*; they invalidate the attested
checkpoint bundle that artifact imports, because the bundle names
`sha256:1f78d344` and profile `5561d364`.

That makes the ordering for closing this task explicit, and it is longer than
the remaining code work:

1. Finish the runtime dimension in the compiler (the `+62`, `+8` and `+1281`
   families, and the two extra-read/write calls).
2. Re-run the checkpoint builder ceremony for the new compiler identity so a
   bundle exists that a fixed node will accept — the task 134 tooling, with
   builder policy and signatures, not a hand-edited manifest. Rewriting the
   recorded identity in a copied bundle would forge exactly the binding the
   builders signed, so it is not an option.
3. Import that bundle fresh and re-execute the window, comparing every receipt
   to the canonical record. That is what closes this task and unblocks 106.

Two smaller notes from the attempt, both worth keeping. The release follower
artifact deliberately rejects `event_observer` and `rpc_bind` (task 135's
minimal-surface work), so receipt capture needs the full node binary — the
follower cannot be the audit subject. And a binary built in a working tree that
carries another session's uncommitted `clar2wasm` edit reports a different
profile fingerprint than the committed content does (`604cc589` against
`1d7b821a`); anything measuring compiler identity has to be built from a clean
checkout, which is the same trap the sample repin fell into earlier today.

## 2026-08-22T17:05:00Z — a verified window against the chain: 64 of 66

The measurement this task needed now exists, and it did not need a re-attested
checkpoint after all. `xtask replay-window` walks a manifest of consecutive
mainnet blocks through a reflinked scratch: the first is replayed against a
scratch discarded to its parent and each later one continues from the block
before it, so fees, nonces and cumulative cost are the real prefix, and every
committed root is verified as it goes.

First window, 39 consecutive blocks from 8,808,752, 66 transactions:
**64 match the canonical record on all five dimensions.** The 40th block stops
on a matured-tenure accounting boundary the unauthenticated fixture seam does
not carry, which bounds a single window's length and says nothing about cost.

The two that do not match are the same contract and function —
`SM1FKXGNZJWSTWDWXQZJNF7B5TV5ZB235JTCXYXKD.dlmm-liquidity-router-v-1-2`'s
`withdraw-liquidity-multi` — over-charged by 1,228 and 98 runtime units, and on
this shape the interpreter agrees with the chain exactly, so it is a usable
oracle here. Called with a hand-serialized **one-element** positions list at the
same prestate, the over-charge is **2 units**, so it accrues per folded
position: a list of tuples each carrying three trait references. Reduced probes
of that shape — a fold over such a list, delivered as an argument so the
implicit cast happens at admission the way mainnet delivers it — charge
identically, so the reduction is not finished.

Scale, so the residual is not mistaken for more than it is: 2 units per
position against transactions costing 75 and 169 *million*, next to every
length dimension in the window matching exactly. It is still a differential and
still blocks release under the plan's rule, but it is one call shape at a known
magnitude rather than 1,313 unexplained numbers.

A caution for anyone extending the charge probe: the cost meters are
per-instance globals, so a running total is only continuous within one
instance. Crossing a `contract-call?` makes the next charge look like it cost
the whole callee. Attributing costs across calls needs instance tracking that
the probe does not yet do; within one instance the totals are exact, which is
what localized the earlier families.

## 2026-08-22T20:15:00Z — the full chain-level inventory: 793 of 848

Ran the canonical audit across ten windows spanning the hold range
(8,808,800 to 8,814,235), each replayed in its own fresh reflink scratch
discarded to its own first parent so the transaction prefix is real inside the
window. **848 transactions, 793 matching the canonical record on all five
dimensions (93.5%), 55 differing — every one of them runtime-only.** No
read_count, read_length, write_count or write_length difference anywhere in
848 transactions, and no unavailable oracle record.

The 55 fall into twelve call shapes, and they are concentrated:

| count | shape |
|---|---|
| 29 | `dlmm-liquidity-router-v-1-2::withdraw-liquidity-multi` |
| 12 | `stableswap-swap-helper-v-1-5::swap-helper-a` |
| 3 | `psis::r` |
| 2 | `borrow-helper-v2-1-8::withdraw` |
| 2 | `xyk-swap-helper-v-1-3::swap-helper-b` |
| 1 each | `gent::rd`, `wars::rc`, `v0-vault-stx::redeem`, `mia-smart-faktory::smart-buy-with-stx` and `::smart-buy-with-sbtc`, `zr-d-0::r-d`, `pox-5::stake-update` |

Magnitudes are small and go both ways: `+8` on fifteen calls, `-184` on three,
then a tail of `+1930`, `+1002`, `+1245`, `+448`, `+440`, `+106`, `+801`,
`+1900` and others, against transactions costing millions.

Two things to carry forward. The `pox-5::stake-update` entry is the one that
deserves priority regardless of its single occurrence: it is a boot contract,
so it is consensus-critical in a way a third-party router is not, and it is the
only one of the twelve that nano ships the source of.

And the biggest shape is bounded tightly. `withdraw-liquidity-multi`
over-charges by a **constant 2 units per call**, not per position — measured at
1, 2, 3 and 5 positions on real state at the real prestate. Calling the
contract it calls (`dlmm-core-v-1-1.withdraw-liquidity`) directly with the same
seven arguments is exact, and five faithful reductions of the router's own
shape all charge identically: a fold over a list of tuples carrying three trait
references; the same delivered as an argument so the implicit cast happens at
admission; trait values extracted with `get` and passed as trait arguments to a
static call; the response-wrapped `(list 326 {x-amount, y-amount})` accumulator
grown through `unwrap!`/`append`/`as-max-len?`; and all of it together, under
both current and recorded (Epoch 2.5 / 3.0) semantics. So the two units need
something about the real contract graph that the reductions do not carry, and
the next step is instance-aware charge attribution rather than another guess.

## 2026-08-22T22:40:00Z — one named word, on the boot contract

The `pox-5::stake-update` entry from the inventory is now localized to a single
charge, which is the sharpest this residual has been.

Called at its real prestate (parent `34b6c896…`, the real sender and the real
five arguments taken from the transaction), `pox-5.stake-update` charges 31
runtime units more than the reference. The traced streams are small enough to
read whole — 48 charges against 47 — and per-label totals match **to the unit
on all fourteen other labels**: `contract-of` 26,800 both, `eval:type-check`
1,215 both, `eval:variable-size` 1,259 both, `eval:function-lookup` 272 both,
and so on. The entire difference is one `unwrap!` costing 31 that the compiler
charges and the reference never charges at all; `grep` finds no unwrap charge
anywhere in the reference's stream, while other traces of other contracts show
`cost_unwrap_ret` charged normally, so the instrumentation is not blind to it.

The source line is `(current-info (unwrap! (get-staker-info tx-sender)
ERR_NOT_STAKING))`, the third binding of `stake-update`'s `let`, and this
execution ends in an arithmetic underflow at a later binding,
`(num-cycles (- unlock-cycle current-cycle u1))`.

That suggested a general rule — the compiler charging a word before performing
it while the reference charges after, so an abort in between leaves the eager
engine having charged something the other never reached, which would explain a
residual that only appears on failing transactions and why every earlier
reduction matched by succeeding. Four probes test it and all four charge
identically: an abort after an `unwrap!`, an abort in a later binding of the
same `let` after an `unwrap!` of a user-defined call, an arithmetic underflow
inside a called function, and one inside a fold. So the rule as stated is not
it, or the harness does not reproduce whatever makes it visible.

One caution about this measurement, recorded because it would be easy to
over-read: the real transaction *succeeded* and differs by 103, while this
direct call *aborts* and differs by 31, so they are two divergences on one
function rather than the same one seen twice. What is solid is that a single
`unwrap!` charge accounts for the whole 31 with every other label matching
exactly, on a boot contract, reproducibly, from real state.

## 2026-08-22T23:55:00Z — correcting the previous entry's framing

The entry above calls the `pox-5` measurement "one named word, on the boot
contract" and treats it as the sharpest form of this task's residual. The
measurement is right and the localization is right — a single `unwrap!` costing
31, with all fourteen other labels matching to the unit, and the reference's
stream contains no unwrap charge under any capitalization (checked
case-insensitively, and the full list of cost names it does charge is
`add contract_of fetch_entry fetch_var if inner_type_check_cost let
load_contract lookup_function lookup_variable_depth lookup_variable_size match
sub tuple_get user_function_application`). The *framing* is wrong, in a way
that matters.

That direct call **aborts** with an arithmetic underflow. The real transaction
**succeeded**, and its canonical delta is 103. So the two are different
executions of the same function, and the 31 is a compiler-versus-interpreter
difference on a code path **the chain never executed** — which means it has no
canonical arbiter, and this task has already established that where the two
engines disagree the interpreter is not automatically right. It may not be a
defect at all.

What is canonical about `pox-5::stake-update` is the +103 on the succeeding
path, and that path cannot be reduced with the tools used above: reaching it
needs the block's transaction prefix, so `cost-both`, which runs one call
against a parent, takes the aborting branch instead. Tracing it needs the
charge probe inside a full-block replay, and the reference has no full-block
replay to compare against — which is the real obstacle to reducing any of the
55, not the absence of a reduction idea.

Two corrections to carry forward, then. Any per-call measurement taken with
`cost-both` must be checked against whether the chain took that path before it
is called a divergence; the prestate alone does not guarantee it. And the
canonical residual for this task remains exactly what the ten-window audit
says: 55 of 848 transactions, runtime-only, across twelve call shapes, of which
`pox-5::stake-update` is one at +103.

## 2026-08-23T02:30:00Z — a real fix from the canonical audit: 822 of 848

The index-0 observation is what unblocked this. A transaction at index 0 in its
block has no prefix, so `cost-both-tx` against the parent reproduces the chain's
exact path — 11 of the 55 differing transactions were at index 0, and one of
those (`0xf8985deb` at 8,813,012) was the only transaction in its block, making
its canonical comparison exact.

That transaction charged 402 runtime units too many, its compiler-versus-
interpreter delta was also exactly 402 (so the interpreter is the oracle on this
path), and the before/after meter probe localized it cleanly: 150 `concat`
charges, **equal counts on both engines**, compiler total 5,052 against the
reference's 4,650. Pairwise, the compiler passed n=4,400 where the reference
passed 275 — exactly sixteen times, the in-memory width of a `uint`.

**`concat` was charged for the bytes its memory copies moved, not for the items
it joined.** The reference's `special_concat` accumulates `seq.len()`, which is
items; nano accumulated the byte span it needed for `memory_copy` anyway. A
buffer or an ASCII string hides this completely — an item there *is* a byte —
which is why every existing `concat` crosscheck was green. Fixed by dividing by
the element stride for lists and by four for UTF-8 strings, with probes pinning
lists of `uint` and `int` and a UTF-8 string.

Re-running the same ten windows measures it: **822 of 848 transactions now match
the canonical record on all five dimensions, up from 793.** The fix closed 29 of
the 55, more than half, and windows 7 and 9 are now perfect. Residual: 26
transactions, 3.1%, still all runtime-only.

What is left, and why it is harder. The remaining magnitudes are `+8` on
fourteen transactions, then `-184` on three, `+1002` on two, and singletons
(`+1602`, `+1569`, `+103`, `+66`, `+33`, `-92`, `-88`). Tracing a `+8` at index
0 shows no label with the concat signature — no label has equal counts and
unequal cost. Instead the compiler makes 1,633 *more* charge events than the
reference across many labels (+260 `eval:variable-size`, +11 `contract-of`, +88
`eval:type-check`, +497 `eval:function-lookup`), whose costs nearly cancel to
leave 8. The two engines decompose the same work into different charge events
and the tables were tuned so the sums agree; where they do not agree by a few
units, a label-level diff cannot say which event is wrong. Closing those needs a
structurally aligned comparison — matching charge events to source positions
rather than to labels — which is the next instrument to build.

## 2026-08-23T04:15:00Z — the position probe, and the limit it exposes

Built the instrument the previous entry asked for: both engines now report
*where* each charge belongs, not just what it was. The compiler tracks the
`SymbolicExpression` it is traversing and emits an interned `contract#id`
before each charge; the reference prints the same pair at every `eval`. So a
charge stream can be grouped by source position instead of by label, which is
what a comparison needs when the two engines decompose the same work into
different numbers of events.

It immediately shows two things, one useful and one disqualifying.

The useful one: the engines attribute a charge to *different nodes* — the
compiler to the expression it is traversing, the reference to the innermost
`eval` — so positions come in offsetting pairs (`stableswap-core#868` at
+364,800 against `#1121` at −362,016 for the same work). Any positional
comparison has to normalize that, by parent or by contract.

The disqualifying one: **the extracted per-charge costs sum to more than the
transaction actually cost** — 6,669,529 against a real 6,515,639 — with no
negative deltas and no outlier (the largest single charge is a legitimate
13,400 `contract-of`). So the arithmetic is not corrupted; the meter is. The
host syncs the cost globals between caller and callee instances, and a caller's
charges that have not yet been flushed to the tracker when a callee's instance
is seeded get counted twice. Which means before/after meter differencing is
**exact within one instance and unreliable across a nested call**.

That retroactively explains which of this task's findings were safe. `pox-5`
(one contract) and `concat` (150 charges in one contract, compared per label
with equal counts) were both in the exact regime, and the `concat` fix verified
against the canonical record to the unit. The `+8` family spans six contracts,
so it is squarely in the unreliable regime, and the per-contract deltas it
produces (+185,033 on `stableswap-core-v-1-4`) are extraction artifacts rather
than findings — recorded explicitly so nobody chases them.

Closing the remaining 26 therefore needs the meter made exact across calls:
flushing the wasm globals to the tracker at every charge in a traced build, so
that no charge is ever counted in two places. That is a contained change to the
traced-build path and it is the next step, ahead of any further reduction
attempts.

## 2026-08-23T05:00:00Z — what the position data says, and where it stops

Two mechanisms in the extraction are now identified rather than suspected.

The inflation is `run_free`. A callee executed through `ExecutionState::run_free`
— `short_circuit_cost` in the `contract-call?` host function — still decrements
the callee module's cost globals, because generated code always does, while the
tracker is never charged. My probe measures the globals, so it counts work the
transaction did not pay for. In the `+8` transaction those regions are 583,502
units across 24 positions the reference never charges at all, which is most of
the 153,890 by which the extracted sum exceeded the real runtime.

What remains after excluding them is not yet a comparison. The two engines
attribute a charge to different nodes — the compiler to the expression it is
traversing, the reference to the innermost `eval` — so the same work appears as
offsetting pairs: `stableswap-core#1121` is 362,016 *low* on the compiler side
while its neighbours are correspondingly high, and the net over shared positions
is −84,667 rather than the +8 the transaction actually differs by. Positional
comparison needs the attribution normalized (fold each charge up to a common
ancestor, or compare subtree totals) before an 8-unit difference inside 6.5
million across six contracts can be read off it.

So the specified next step is now two things, not one: identify and exclude
`run_free` regions (the host knows when it enters one and can say so through the
same probe), and normalize attribution to subtree totals. Both are contained
changes to the traced path. Until they exist, this task's reliable findings come
from single-contract transactions and from per-label comparisons where the event
counts agree — which is how `concat` was found and fixed, and it is worth noting
that that one fix closed more than half the canonical residual.

## 2026-08-23T06:30:00Z — retracting the run_free diagnosis

The entry above blames the extraction's inflation on `run_free`: a
short-circuited callee decrementing the module's cost globals while the tracker
goes uncharged. I instrumented the boundary to confirm it — the
`contract-call?` host function now announces `free-enter`/`free-exit` when
`short_circuit_cost` is taken — and re-traced the `+8` transaction.

**Zero free regions.** The transaction never takes that path, so `run_free`
cannot explain anything about it. The diagnosis was wrong and is withdrawn; the
marker stays, because it costs nothing and the next person to suspect free
regions can rule them out in one run instead of an afternoon.

What is established about the extraction, stated as measurement rather than
mechanism:

- On a **single-instance** trace it is exact. `pox-5.stake-update`'s 48 charges
  sum to 30,278, its `cost_load_contract` is 137,147, and 30,278 + 137,147 is
  167,425 — the runtime to the unit.
- On a **multi-instance** trace it over-counts. The `+8` transaction extracts
  6,669,529 where the wasm-side total should be about 6,275,639 (6,515,639 less
  roughly 240,000 of host-side contract loading), so about 394,000 too much,
  spread across no outlier and with no negative delta anywhere.

Ruled out so far: free regions (measured, zero), arithmetic corruption (no
negative or oversized deltas; largest single charge is a legitimate 13,400
`contract-of`), and probe mis-pairing on branch boundaries (the four probe
emissions per charge all go into the same instruction sequence).

Still unexplained, and this is the blocker for the remaining 26: why summing
per-charge deltas across instances exceeds the transaction's wasm-side cost.
Until that is understood the multi-contract traces cannot be trusted, and the
landmark-aligned cumulative walk built on top of them drifts to −187,668
instead of converging on the +8 the transaction actually differs by. The next
step is to instrument the *instance boundary* itself — report the meter on both
sides of every seed and write-back — rather than to attempt another reduction.

## 2026-08-23T07:30:00Z — I instrumented dead code, twice

`link_contract_call_fn` at `linker.rs:6087` — the function whose
`short_circuit_cost`/`run_free` branch and whose `remaining_costs` /
`set_remaining_costs` hand-off both entries above reason about — carries
`#[cfg(any())]`. That is `cfg(false)`: permanently disabled. There is a second,
live `link_contract_call_fn` at `linker.rs:6323`, and it is the one registered
in `link_host_functions`.

So both markers I added measured nothing, and the "zero free regions" that made
me retract the `run_free` diagnosis was zero because the code cannot run, not
because free regions do not occur. The retraction stands — that diagnosis was
never demonstrated — but its stated evidence was worthless, which is worse than
having no evidence, and the markers are removed rather than left where they can
mislead again. The give-away was available and I missed it: `cost_meter`, the
field that branch assigns, does not exist anywhere in the clarity revision this
tree builds against.

What reading the *live* path does establish is the accounting model, and it is
not the one I assumed. A nested call runs in an instance whose cost globals
start at `i64::MAX`; at exit, `call_function_with_argument_sizes` computes
`CostMeter::used_from_remaining(remaining)` and returns that as the call's cost.
So instances do not share a meter and are not seeded from the caller —
each reports what it used, and the tracker sums them. Whether the same instance
can serve nested calls without its inner call being counted twice is the precise
question to answer next, because that is the only mechanism left that would make
per-charge deltas summed across instances exceed the transaction's wasm-side
cost, which is what the `+8` trace does by about 394,000.

The lesson worth keeping, and the reason this entry exists at all: before
trusting an instrument, check that the code it instruments runs. A marker that
never fires reads exactly like a measurement of zero.

## 2026-08-23T09:00:00Z — the instrument is faithful; the gap is internal accounting

Third try at the right function, and this one is live: the call path's cost is
computed at `initialize.rs`'s `call_function_with_argument_sizes`, where each
instance's `CostMeter::used_from_remaining(remaining)` is added to
`global_context.cost_track`. Marking that gives three numbers for the `+8`
transaction, and they settle what I had wrong:

| what | runtime |
|---|---|
| sum of my extracted per-charge deltas | 6,669,529 |
| **sum of the engine's own per-instance `used` reports (33 instances)** | **6,669,529** |
| the receipt | 6,515,639 |

My extraction equals the engine's own instance accounting **exactly**. So the
probe is faithful and the earlier entries were wrong to call it inflated — what
they should have said is that the *sum over instances* is 153,890 higher than
the receipt, which is a question about nano's cost plumbing rather than about
the instrument.

One more measurement, uninterpreted on purpose: `LimitedCostTracker::add_cost`
is called 66 times in that run, exactly twice per instance, totalling 7,292,020.
That is not necessarily a defect — `cost-both-tx` builds trackers per engine and
free trackers exist for read-only work, and my print does not distinguish which
tracker it is watching — so the doubling is a lead, not a finding, and it is
recorded as such.

Where that leaves the remaining 26. The multi-contract traces can be trusted
after all, which is progress, but the numbers to compare are per-instance
`used` values against the receipt rather than charge sums against each other,
and that comparison needs the receipt's own derivation understood first. The
concrete next step is to find where the 153,890 goes: sum the instance reports
per contract, compare with what the receipt attributes, and identify which of
the 33 instances is not represented in it.

And a process note after three misdirected instrumentations in a row — the
disabled `link_contract_call_fn`, the deployment path, and then the right one:
when a marker reports zero, the first hypothesis should be that the code does
not run, not that the phenomenon is absent. Each of those cost a build and a
trace, about six minutes, and the reading that would have prevented all three
was five lines of `link_host_functions`.

## 2026-08-23T09:45:00Z — closing this thread, with the numbers that stand

Followed the 153,890 gap one step further and stopped, because the step does not
support a conclusion. The 33 instances are one top-level call of 6,149,713 and
32 nested ones summing to 519,816; the receipt minus the top-level is 365,926,
and 519,816 − 365,926 is exactly the gap, which is consistent with part of the
nested work being counted both inside its parent's meter and again in its own
report. A subset of the nested values does sum to 153,890 — but it has twenty
elements, eight of them 52, and a subset-sum over thirty-two values with that
many small ones will hit almost any target. **That is not evidence and is
recorded so nobody treats it as such.**

What stands from this thread, all measured:

- The charge probe is faithful: its per-charge sum equals the engine's own sum
  over instances, 6,669,529 both.
- That sum exceeds the receipt by 153,890, and the shape of the discrepancy
  points at nested work being counted twice — in a parent's meter and in its own
  report — which is a question about nano's cost plumbing, not the instrument.
- `add_cost` fires exactly twice per instance, which may be free trackers the
  print cannot distinguish.

And what closes the remaining 26 is now a specific reading task rather than more
tracing: follow one nested call from `link_contract_call_fn` at `linker.rs:6323`
through `call_function_with_argument_sizes` and establish whether the callee's
consumption also decrements the caller's globals. If it does, the parent's
`used_from_remaining` double counts it, the receipt must be compensating
somewhere, and *that* compensation is where a few units can go missing. If it
does not, the gap is elsewhere and the instance reports are simply not what the
receipt sums.

Four misdirected instrumentations in this thread — a `cfg(any())` function, the
deployment path, then the live one, then a subset-sum that proves nothing — all
share a cause: I was measuring before reading. The next person should read that
one call path end to end first.

## 2026-08-23T10:30:00Z — resolved by reading: nothing is double counted

Did the reading task instead of another measurement, and it dissolves the
153,890 rather than explaining it.

The reported cost is `global.cost_track.get_total()`, read at
`nano-vm/src/lib.rs:5091` after the call commits. The tracker it reads is the
one seeded from the `cost_tracker.clone()` passed into the call — a **clone**.
Charges made against other trackers, including the clones nested and read-only
work receive, never reach it. So the receipt was never supposed to equal a sum
over every instance: my aggregation was adding up charges from several
trackers, only some of which feed the number in the receipt.

So the accounting is sound, my instrument is faithful per instance, and the
three earlier framings of this gap — inflation, `run_free`, double counting —
were all wrong for the same reason: I was comparing totals that belong to
different trackers. Every one of those entries above is superseded by this one.

What that means for the remaining 26, stated as the actual next step:

- A charge trace is exact **within one instance**, and instance sums are only
  comparable to a receipt when every instance feeds the tracker the receipt
  reads. Localizing an 8-unit difference needs the charges filtered to that
  tracker, which the probe does not yet report; the tracker identity would have
  to travel with the charge the way the position now does.
- The alternative, and probably the cheaper one: keep using
  single-transaction blocks and index-0 transactions, where the receipt is one
  call's own cost and per-label comparison with matching counts is decisive.
  That is how `concat` was found, and eleven of the original fifty-five
  differing transactions were at index 0 — several of the remaining families
  should be reachable the same way without any new instrumentation.

Recording the shape of my own error here because it repeated four times in one
thread and the pattern is the useful part: each time I measured a quantity,
found it did not match another quantity, and proposed a mechanism — instead of
first establishing that the two quantities were supposed to be equal. Three of
the four proposals were withdrawn. The reading that settled it took ten minutes.

## 2026-08-23T11:15:00Z — the +33 case, localized to one charge and one expression

Applied the cheaper path the reading suggested, and it works as a *localizer*
even where it does not yet yield a fix. Transaction `0x79b45bc7…` at height
8,811,204 (`SP1A27KFY4XERQCCRCARCYD1CC5N7M6688BSYADJ7.v0-vault-stx::redeem`) is
at index 0, differs from the canonical record by 33 runtime units, and runs in
only two instances — the regime where a charge trace is exact. Its per-label
diff has exactly two rows: `>` against `>=` at ±10 events and ±380 units, which
is my name map cancelling itself, and **one extra `eval:variable-size` worth
+33**, which is `2 × 16 + 1` — a single copy charge on a 16-byte value.

Event-by-event alignment places it exactly. The compiler charges
`variable-size(16)` at expression `v0-vault-stx#1276`, immediately before
`append` with n=16 at `#1273` and `as-max-len?` at `#1271`. Fetching the source
names the function:

```clarity
(define-private (iter-unpack-u16 (pos uint) (acc {word: uint, fields: (list 8 uint)}))
  (let ((word (get word acc))
        (fields (get fields acc))
        (unpack (unpack-u16-at word pos))
        (new (as-max-len? (append fields unpack) u8)))
    { word: word, fields: (unwrap-panic new) }))
```

So the charge is for reading `unpack` — a `let`-bound `uint` holding a function
call's result — as `append`'s second operand. `append` is a special function in
the reference, which evaluates its own operands and reads them through
`as_ref`, so it charges no copy. That is the same class as the three fixes
already landed for the block-info words, `to-ascii?` and `secp256k1-verify`.

What it is not, yet, is reproduced. Eight reductions of the shape all charge
identically to the reference: `(append (list u1) x)` with a parameter; both
operands `let`-bound; the same through a private function; the verbatim
`iter-unpack-u16` including the tuple accumulator, the call-bound item and the
`fold` driving it; and that verbatim form again under Epoch 2.1/Clarity 1 and
Epoch 2.4/Clarity 2 recorded semantics, since the contract was published at
height 6,162,060. So the trigger needs something from `redeem`'s wider
execution that a standalone fold does not carry.

That is where I am stopping, and the handover is as narrow as I can make it: one
contract, one private function, one word, one 16-byte operand, 33 units, five
expression identifiers, and eight shapes already excluded. The next attempt
should start from `redeem` rather than from `iter-unpack-u16`.

## 2026-08-23T12:30:00Z — a caveat on the expression mapping, and the ninth exclusion

Two corrections to the entry above, both of which would otherwise be inherited
as fact.

The ninth reduction is excluded. `v0-vault-stx` drives its fold over a
`define-constant` list — `(fold iter-unpack-u16 ITER-UINT-8 init)` where
`ITER-UINT-8` is `(list u0 u1 u2 u3 u4 u5 u6 u7)` — and a constant is read
through `lookup_constant_variable`, a different compiler path from the list
literal every earlier reduction used. Replicating that, constant and all,
still charges identically to the reference.

And the mapping I asserted is not established. I wrote that the extra
`variable-size(16)` at `v0-vault-stx#1276` is the read of `unpack` as `append`'s
second operand. What is solid is that the contract contains **exactly one**
`append`, so `#1273` is that expression. What is *not* solid is that `#1276` is
`unpack`: expression identifiers are assigned by a depth-first walk, so
`(append fields unpack)` plausibly numbers the call, then `fields`, then
`unpack` — which would put the operands at `#1274` and `#1275`, and leave
`#1276` as something else, perhaps `as-max-len?`'s `u8`. The trace also shows a
`variable-size(6)` at `#1275` immediately before, and 6 is not a size any
operand of this expression obviously has, which is the sort of detail that
should have made me check rather than narrate.

So the honest state of this lead: one contract, one `append` expression
(`#1273`), one extra 16-byte copy charge in its immediate neighbourhood worth 33
units, nine reduction shapes excluded — and the specific binding being copied is
**unidentified**. Establishing it needs the contract's AST numbered rather than
guessed: compile it with the probe on and print the interned position for each
expression alongside its source form, which the generator can do because it has
both.

## 2026-08-22T23:27:25Z

2026-08-22/23: three charging defects closed, found by aligning charges to
source positions rather than to labels — the method the previous entry said
was needed.

**Instrument.** The charge probe now names the expression a charge belongs to
(`{contract}#{id} {form}`, commit 0ea04532), which is what made attribution
possible without counting `SymbolicExpression` ids by hand. The reference side
comes from an out-of-tree instrumented clone of the pinned clarity revision
(`/home/aldur/stacks-core-cost-trace`, efc34a07 + eprintln only, no behaviour
change) wired in by a `[patch."https://github.com/stacks-network/stacks-core.git"]`
in *both* the root and `vendor/clarity-wasm` workspaces; `NANO_TRACE_CHARGES`
gates positions, `NANO_TRACE_COSTS` gates charges. Never committed.

Caveat, measured: for a multi-contract transaction the compiler probe stream
over-counts events (1,261,198 traced against the engine's reported 611,863 on
one nine-contract call), so comparing *counts* is useless there. Comparing the
set of (position, label, input size) triples is not: on that same transaction
every input size matched except two, and both were real defects.

**with-stx's allowance amount is read borrowed** (20ca3652). `eval_allowance`
reads `with-stx`'s single operand through `as_ref` and takes every `with-ft`,
`with-nft` and `with-staking` operand through `clone_with_cost`. Nano charged a
copy for all of them: one extra `LookupVariableSize(16)` = 33 units per
`(as-contract? ((with-stx amt)) ...)` over a bound uint. Audit 822 -> 824/848.

**A tuple is sized from its values' types** (b1d039bb). A tuple's size embeds
its own type size and the reference derives that from `type_of(value)`, so a
`none` field contributes `NoType`, not the declared `(optional (string-ascii
40))`. On `principal-destruct?` of a standard principal that is 39 against
nano's declared 43, and `2n + 1` makes it +8 — the family carried by fourteen
of the twenty-four then-differing transactions. Optionals and responses are now
measured from the discriminant the generated code already branches on; a field
that cannot reach sends the tuple to the host, which materialises it and calls
the reference's own `size()`.

A callable must *not* go that way, and the first version of this fix sent it
there: a trait reference erases to a bare principal through memory and comes
back sized 148 where the reference says 276. Six transactions regressed by
multiples of 256 (-2816, -3072, -2048, -1024, -768). Attributed by replaying
block 8,808,800 both ways: 27,513,287 — the chain's number — with the callable
left alone, 27,510,471 without. A synthetic trait-bearing tuple does not
reproduce it, because such a tuple always carries a runtime-shape handle and
takes the host path either way; the code comment is the guard.

**A match arm looks its reads up at its own depth** (1d13861f). Both arms of a
response match bind a name, so the reference evaluates both one context deeper;
nano restored the binding table between them from a snapshot taken before
entering that scope, charging the error arm one level too shallow. An optional's
`none` arm is the opposite case — `special_match_opt` evaluates it in the
enclosing context — and charging it one deeper was the first thing the fix did,
which its pin caught.

Each defect has a probe in `borrowed_operand_charges` (48 passing, 1 ignored),
and each was verified to fail without its fix.

**Where the audit stands.** With the first two fixes: 832/848, sixteen
differing, six of them the trait regression now closed. The full ten-window
re-run with all four commits is in flight. Of what remained, `-184` on three
transactions is the `swag`/`psis` shape this task already records as the one
where the interpreter is further from the chain than the compiler, and several
residuals are multiples of 128, which is the mirror of the erasure above: a
callable carrying no trait identifier that nano sizes 276 and the reference
sizes 148. That is adjacent to task 147's territory and is the next lead.

## 2026-08-23T01:21:54Z

2026-08-23: two more defects closed on `from-consensus-buff?`, a third
characterized exactly, and the canonical audit measured at 838/848 with all
four local gates green on 16b779c7.

**Tooling.** `cost-both-tx` takes an optional block id and begins its block
from that block's recorded headers (c6e8bac7). The runner used to start at
Bitcoin height zero, so `pox-5::stake-update` aborted with an arithmetic
underflow where the chain's call succeeded — comparing engines on that path is
how the retracted boot-contract finding happened. With the recorded view,
block 8,813,016's transaction charges 6,374,986: the receipt's own number, with
no transaction prefix, because the prefix never mattered here. The burn view
did.

**`from-consensus-buff?` borrows its bytes and pays for its type** (16b779c7).
Two halves, which is why the earlier attempt recorded itself as open rather
than fixed. The reference reads the buffer through `as_ref`, so nano's operand
copy was never charged; and it parses the type argument first, charging a
`TypeParseStep` per node it recurses through, which nano charged not at all.
Dropping only the copy left it 4 under — one unparsed `uint` — which is exactly
what the ignored probe had measured, and that probe is now un-ignored and
green. On mainnet the type is five nodes (20 units) against a 219-unit copy of
a 105-byte binding.

**Open, recorded with its measurement: a deserialized value keeps the declared
widths of its sequences.** `try_deserialize_bytes_exact` builds the value
against the type it was given, so a `(buff 32)` field holding 20 bytes still
sizes as 32. Measured directly: the same bytes size 164 constructed and 176
deserialized, sanitized or not. Nano measures the representation it holds, so
it charges the 164 — 72 units across two reads and one argument type-check on
`xverse-signer-manager-3`, which is the whole of what is left of that
transaction's difference (6,374,811 against the chain's 6,374,883). Closing it
needs provenance nano does not track: a map or variable read deserializes
against its declared type too, so the rule is not local to the word, and the
rule is not simply "declared" either — an absent optional in a deserialized
value still contributes `NoType`. Recorded as an ignored probe naming this task.

**Where the ten that remain are.** No regressions from any of the day's five
commits; the pox-5 transaction moved from +103 to -72 and the rest are
unchanged: `-184` on three, `+1536` on two, `+24` on two, and `-88`, `-92`,
`-72` singletons.

The `-184` family is `psis::r`, and it now reproduces offline: at the recorded
burn view the compiler charges 57,053,667 against the interpreter's
57,053,851, exactly the canonical delta, so the interpreter is a usable oracle
for this shape after all. A 90 MB dual trace narrows it to one one-sided
eval-level charge — nano reads `arkadiko-swap-v2-1`'s `pair-updated` (623
bytes) with a copy the reference never charges, four times. That cannot be the
whole story, because it would put nano 4,988 *over* rather than 184 under, so
something compensating sits in the word-level charges the label mapping does
not yet cover. Naming it rather than acting on it: fixing the read alone would
make this transaction worse.

`+1536` is 12 x 128, which is the mirror of the trait erasure fixed in
b1d039bb — a callable that carries no trait identifier sized 276 where the
reference says 148. Nano's runtime representation of a callable erases the
identifier entirely (`type_for_serialization` maps every callable to
`PrincipalType`), so it cannot branch on it today; that is adjacent to task
147's ABI work.
