## 2026-08-17T07:45:53Z

Started after Task 129 closed. The execution boundary already retains authoritative per-transaction ExecutionCost in TransactionReceipt, and the stock new_block SSE payload already exposes all five raw dimensions. The remaining correctness gap is budget identity: nano-tui currently compares receipts to /v2/pox current_epoch at viewing time. The existing payload carries burn_block_height and /v2/pox carries epoch start/end intervals, so the minimal fix is to resolve the governing interval by execution burn height and show unavailable if no exact interval/limit exists; no re-execution, consensus-state expansion, archive, or new route is needed.

## 2026-08-17T07:57:29Z

Completed in commit 7653f40e. Audit confirmed AppliedBlock retains TransactionReceipt and nano-rpc's stock-compatible new_block payload already publishes each receipt's authoritative five-dimensional ExecutionCost, so the conditional retention task required no new storage or execution. The payload also carries burn_block_height and /v2/pox carries explicit epoch start/end intervals and block limits. nano-tui now selects the interval containing the receipt's execution burn height, labels that epoch and height, computes six-decimal percentages with exact integer arithmetic, and reports the applicable limit unavailable when no exact interval/limit exists. It never uses current_epoch for a historical receipt. The view states that the five limits are independent and has distinct zero-cost, missing-limit, missing-receipt and historical-current-mismatch regressions. Final gates: nano-chainstate 35/35; nano-rpc 69/69 runnable with its one declared live-Hacknet test ignored; nano-tui 38/38; strict all-target Clippy across all three packages green; rustfmt and diff-check green.
