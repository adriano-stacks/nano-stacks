## 2026-08-11T12:02:34Z

Reproduced the mandatory workflow locally. The first failure is vendor formatting under pinned rustfmt 1.9.0; applying the formatter and continuing through every offline gate.

## 2026-08-11T14:28:28Z

Fixed the dual-workspace formatting gate, strict clippy type complexity, stale principal/serialization test ABIs, and the execution-only fixture validation mismatch. Registered authentication-only ignores in release policy; focused regressions and policy inventory are green. Final exact workspace and release-evidence gates remain.

## 2026-08-11T18:26:13Z

All pinned offline CI gates are green: dual-workspace formatting, strict release/all-target clippy, 340-block scoreboard, fixture integrity, 283 active conformance tests, the full release workspace, and release evidence with the required exit status 2. Added focused regressions for formatter coverage, execution-versus-release fixture qualification, cross-worktree compiler identity, journal DBMOVED retry, and catch-up progress semantics; lockfiles remain unchanged.

## 2026-08-12T06:38:32Z

Reopened after hosted CI exposed a workflow-environment mismatch: `CARGO_TERM_COLOR=always` added ANSI bytes to `cargo tree`, so the feature parser returned an empty set despite passing without the workflow environment locally. Reproduced the exact assertion before changing it.

## 2026-08-12T06:50:39Z

Fixed the hosted-only failure by making the release dependency helper reproduce CARGO_TERM_COLOR=always locally and invoke cargo tree with --color never. The focused assertion failed before the patch and passed after it; the full conformance gate under the workflow environment passed 283/283 active tests, and strict workspace clippy passed.

## 2026-08-12T07:29:11Z

Reopened again after commit 16d09e28 reached origin/main unformatted and hosted run 31573295388 failed at the formatting step. The task claimed pre-push protection that was not actually installed. Adding one checked-in gate driver shared by Actions and a tracked pre-push hook, with clean-tree and stable-HEAD enforcement.

## 2026-08-12T07:31:12Z

Formatted the committed default-to regression, proving hosted run 31573295388's formatting failure locally. Added scripts/ci.sh as the single source for every offline Actions gate, a tracked full-gate pre-push hook, clean-tree/stable-HEAD enforcement, automatic hook installation from the pinned Nix shell, and shellcheck coverage for the scripts.

## 2026-08-12T08:51:10Z

The first full pre-push enforcement run passed every offline gate, but took over an hour because Cargo auto-enrolled the vendored clar2wasm path dependency into the root workspace and linked every vendor test with root ThinLTO/codegen-units=1. SSH closed while the hook ran, so the push exited 141 and origin/main stayed at 16d09e28. Moving exhaustive verification to pre-commit, recording the verified Git tree, making pre-push a fast tree check, and separating root/vendor test profiles.

## 2026-08-12T09:18:14Z

A cold test build confirmed the remaining root harnesses and examples were still paying production ThinLTO; stopped it after 17:13 rather than waste another hour. Added an optimized, non-LTO CI test profile, excluded non-test examples, removed the duplicate conformance-suite run, and retained all library, binary, integration, documentation, and vendor tests.

## 2026-08-12T09:47:18Z

Consolidated the root tests under an optimized non-LTO CI profile, removed non-test example links, and kept the full conformance suite once. The exact test gate passed in 14m08s: 284 active conformance tests, every root unit/integration/doc test, 1,475 active vendor library tests, all vendor integration/property/OOM suites, and vendor docs; zero failures.
