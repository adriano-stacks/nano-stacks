## 2026-08-19T11:20:00Z

Measured the decision path before designing over it. The survey (full call
chains and line references) established: the crate graph below
`ChainState::commit_authenticated_nakamoto_block` already has zero
Tokio/HTTP/socket dependencies; `AuthenticatedBlock` is already the persisted
staging envelope; `new_block_payload` + `receipt_digest` are the only complete,
tested serialization of the output side; the store level already has a real
read-only capability (`immutable=1`, no `-shm`); and the isolation task 135
proved is packaging, not typing — the dev node hands one
`Arc<Mutex<CheckpointExecutor>>` to RPC as both `BlockAdmission` and
`ChainAccess`. Four impurities sit inside the path and must move with the cut:
`bitcoin.block_at()` inside `authenticate`, `seed_burn_headers` reading Bitcoin
and writing chainstate mid-path, `NANO_TRACE_*` env reads per Clarity write,
and `NANO_DUMP_WASM*` arbitrary-path file writes (the only decision-path
filesystem access outside the state directory). The `PoxInfo` constants in
every `BitcoinBlockContext` still come from a peer's `/v2/pox` — under a
firewall they belong to the consensus profile or configuration.

docs/epoch4-consensus-firewall.md records the decision: cut between
authentication-context assembly and block application, request =
`AuthenticatedBlock`, response = decision record derived from `AppliedBlock`,
child process promoted from the replay-blocks shape whose SIGKILL/fault
harnesses already exist, typed refusals replacing `Display` strings, shadow
mode compared by receipt digest over the five-boundary offline corpus before
any authority switch, and no retained fallback after it. This checks the
architecture-decision task; extraction starts with the `epoch4-consensus`
types.

## 2026-08-19T12:10:00Z

Four decision-path impurities from the survey are closed on main, each its own
commit with focused regressions: 76f1b3d2 hoists the per-Clarity-write
NANO_TRACE_* getenv calls to one LazyLock read per process; 36480352 removes
the NANO_DUMP_WASM* arbitrary-path file writes from the compile path,
carries refused module bytes to the diagnostic caller through
ModuleInspection, and moves the artifact gate's string-table control to
NANO_TRACE_WRITES; be24958e takes the burnchain fetch out of authenticate —
one Bitcoin fetch per staged block for all its representations, decoded
operations into authenticate_with_operations, and a fetch failure distinct
from a rejection so it cannot cost a staged branch; 969b715b pins mainnet's
PoX origin, phase geometry, slot count and activation height to the
executable profile, verifying every peer /v2/pox answer against them at
bootstrap and per round, adopting only the peer's burn tip, and refusing a
contradicting peer by name. Also 9eabd5de gives health/metrics a per-block
executed height beside the per-round snapshot. Remaining from the ADR:
seed_burn_headers carried in the request, typed refusals, the
epoch4-consensus request/record codecs, the supervised executor process,
shadow mode and the authority switch.
