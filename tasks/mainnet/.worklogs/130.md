## 2026-08-15T21:44:47Z

2026-08-15: Initial production inventory. Cargo.lock resolves Wasmtime 15.0.1 while nano-vm, nano-wasm-cache, vendored clar2wasm and nano-node build identity still say 15.0.0. ModuleCache::default is the production engine constructor; native cache keys already include precompile_compatibility_hash plus emitted Wasm, but the cache format/config identity is not explicit. Current default features pull in cache, async/fibers, profiling/debug support, component machinery and other unused surface. Epoch 4 modules require Cranelift, externref, SIMD, bulk memory, multi-value and one fixed memory/table; searches found no threads/atomics, relaxed SIMD, memory64, multi-memory, tail calls or component model. The pinned RustSec audit is red on 17 Wasmtime vulnerabilities plus separately classified dependency warnings, so no advisory exception is granted. Work begins with an exact supported LTS pin, one explicit engine/config identity, minimal features, store limits and cache invalidation before corpus evidence.

## 2026-08-15T22:37:56Z

2026-08-15: Patched-LTS and shared-engine checkpoint. Wasmtime is exactly 36.0.13 in nano-vm, nano-wasm-cache and clar2wasm with default features disabled. The sole production ModuleCache now constructs a checked-in Cranelift/Null-GC engine; its complete feature/proposal configuration is embedded in compiler/release identity and the native-cache key. Native cache format 2 invalidates earlier artifacts, while compatibility hash + config identity + Wasm bind engine, target, settings and compiler output. Engine tests load standard.wasm and reject multi-memory, memory64, tail-call and shared-memory modules. Exact green gates: clar2wasm engine::tests 3/3; nano-wasm-cache --lib 6/6; xtask release_candidate::tests 6/6; cargo check nano-vm/nano-wasm-cache/nano-node/xtask --all-targets; strict root and clar2wasm all-target Clippy. cargo tree shows only cranelift,gc,gc-null,runtime,std plus their internal required features. Fuel and epoch interruption are explicitly off so host-speed limits cannot become consensus refusals. Commits: 2bde94a4 runtime/API migration; next commit binds explicit engine identity.

## 2026-08-15T22:51:43Z

2026-08-15: Explicit per-Store resource-limit checkpoint. Every production and test Clarity execution Store installs one checked-in limiter: 2 GiB memory (the existing signed i32 host-address boundary), 20 table elements (the exact standard table), and one instance/table/memory. Growth beyond a limit traps; fuel and epoch interruption remain disabled so host speed cannot become a consensus-invalid result. The complete limits are part of ENGINE_CONFIG_ID and therefore release/compiler/native-cache identity. Green serialized gates: clar2wasm engine::tests 4/4, the real as-contract-safe STX path 1/1, initialize::refused_arguments 6/6, vendor all-target strict Clippy, root all-target check and strict Clippy for nano-vm/nano-wasm-cache/nano-node. Nix GC removed only unreachable store paths after the 50 GiB Nix overlay filled, recovering 39 GiB free; repo, protected follower and retained fixture evidence were untouched.

## 2026-08-15T23:05:26Z

2026-08-16: Removed the directly reachable RUSTSEC-2026-0253 instance by pinning nano-sync from lru 0.18.1 to patched 0.18.2. Green serialized gates: nano-sync --lib 21 passed/6 infrastructure ignored; nano-sync all-target strict Clippy. The pinned cargo audit remains intentionally red but fell from six warnings to five: only the TUI ratatui closure and pinned stacks-core/test closure remain. This is a separate atomic checkpoint before those classifications.

## 2026-08-15T23:24:13Z

2026-08-16: Removed the remaining TUI advisory closure by upgrading ratatui 0.29.0 to 0.30.2, disabling its root default feature set, and selecting only the crossterm 0.29 backend plus the rendered-line API nano-tui uses. This removes lru 0.12.5 (RUSTSEC-2026-0253 and RUSTSEC-2026-0002) and paste 1.0.15 (RUSTSEC-2024-0436); the selected layout cache now uses patched lru 0.18.2. Green serialized gates: nano-tui 29/29 and all-target strict Clippy. Pinned cargo audit now reports zero vulnerabilities and two denied unmaintained warnings, both from the pinned stacks-core cross-target/test closure.

## 2026-08-15T23:39:22Z

2026-08-16: Advisory classification/policy checkpoint. The real pinned RustSec database now passes `cargo xtask release-candidate audit`: zero vulnerabilities and exactly two signed, machine-checked exceptions. libsecp256k1 0.7.2 is selected by pinned stacks-core only for target_family=wasm, outside the x86_64-linux/aarch64-linux release targets; net2 0.2.39 is selected only by nano-conformance dev-dependencies, outside stacks-node. advisory-policy.json binds each exact ID/package/version to task 130, a concrete reachability statement and 2026-09-15 expiry. The release candidate carries/checksums the policy and raw audit; unlisted, moved, duplicated, stale, expired, closed-owner or CLI-ignored warnings fail. Green gates: real policy audit PASS; release_advisory 3/3; release_candidate 5/5; full xtask bin 31/31; xtask all-target strict Clippy. This completes the Wasmtime/API/proposal/advisory inventory and mandatory audit-policy gate, while the two migration exceptions remain deliberately time-bounded.

## 2026-08-15T23:41:29Z

2026-08-16: Post-commit clean-tree proof for 15957b31. `cargo test -p xtask --all-targets` is fully green: 31 unit tests, 6 read-only-state integration tests and 7 release-report integration tests. The earlier pre-commit integration attempt correctly stopped at the dirty build-relevant tree boundary; after the atomic policy commit, every expected downstream assertion ran and passed.

## 2026-08-15T23:50:37Z

2026-08-16: Native-cache checkpoint and first-artifact decision. Format 2, emitted-Wasm bytes, Wasmtime compatibility hash and the checked-in engine identity jointly invalidate serialized modules across compiler/runtime/target/settings changes; host imports are relinked from the current binary. Existing task 110 measurements show warming 486 contracts saves about 50 s of user CPU, while the 99-block wall comparison was 24.1 s cold versus 23.5 s warm and the longer range was wall-neutral under concurrent I/O. Because task 135 has not yet run the exact packaged follower catch-up/liveness gate without the cache, the first mainnet artifact will retain the safe miss-through persistent cache; omission is not assumed from an incomplete benchmark. Fixed the cache equality test to discover the current format directory instead of hard-coding format 1. Green gates: compile/cached/recompile equality 1/1; epoch/compiler input invalidation 1/1; nano-wasm-cache 6/6; nano-vm+nano-wasm-cache all-target strict Clippy.

## 2026-08-15T23:58:57Z

x86-64 vendored compiler corpus is green on the explicit Wasmtime 36.0.13 engine: `CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command cargo test --manifest-path vendor/clarity-wasm/Cargo.toml -p clar2wasm --lib -- --nocapture` completed 1,494 passed, 0 failed, 2 intentionally ignored in 389.66s. This covers the compiler result/cost/ABI crosschecks, proposal rejection and Store-limit tests, wide-local stress modules, runtime-shape handling, argument sanitization/refusal, fresh module generation, and native module loading on x86_64-linux.

## 2026-08-15T23:59:44Z

x86-64 node VM corpus is green: `CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command cargo test -p nano-vm --lib -- --nocapture` completed 45 passed, 0 failed, 1 intentional search-only ignore. The corpus covers fresh compilation versus native-cache reload equality, compiler/engine/cache invalidation, malformed-Wasm module-load refusal, recorded-epoch rebuilding without retry, contract refusal fail-closed, transaction rollback and roots, checkpoint state/header loading, and PoX-5 module hydration.

## 2026-08-16T00:00:57Z

x86-64 checked-in capture scoreboard is green with both engines enabled: `CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command env NANO_REPLAY_BOTH_ENGINES=1 cargo run -p xtask -- scoreboard` exited 0 with fixture integrity captured, state roots 340/340, receipts 340/340, all receipt cost dimensions 340/340, frozen mainnet receipt digests 500/500 over heights 8,702,046–8,702,592, and replay depth 340/340. The scoreboard truthfully reports no external `NANO_MAINNET_STATE`; the separate full-state inventory gate remains next.

## 2026-08-16T02:03:49Z

x86-64 full current-tip contract inventory is green at source commit `ba3c51e0bf00`, state tip `63f2beda310a00e9c790f9f8e2e41f42f6f145f034e6cac040cd9bd46746c2b6`, compiler `sha256:c0be3c8ff02eeb2be042e9475f0f6224e42f28eb274bf1ce42a1dfa002c22b6e`. `CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command cargo run -p xtask -- sweep-contracts /home/aldur/mainnet-8716986/state` exited 0: 137,284/137,284 current-tip contracts compile and load; 137,342 distinct candidates from 146,346 metadata rows; 58 proven stale/noncanonical candidates; 0 unmeasured. Exact emitted locals max 1,164/50,000 (48,836 headroom) at `SPXGT7ADNZNARR4SVSJN56QGSZFHGATJEQMFPMJW.pox-4::get-alex`; source maxima 1,003 function params, 1,128 function/control results, 71 control params and 65 top-level results; both contracts above the 1,000 raw boundary lowered and loaded. The separate forced-Epoch40 diagnostic loaded 136,413 and produced 871 expected semantic refusals/0 unmeasured; every one loaded at its recorded deploy epoch in the green current-tip denominator. Full log: `/home/aldur/task130-sweep-x86.log`.

## 2026-08-16T02:18:17Z

AArch64 execution path is proven, not merely cross-compiled. Using isolated `RUSTUP_HOME=/home/aldur/.cache/nano-stacks/aarch64-rustup`, Rust 1.97.1, pinned `cargo-cross` 0.2.5 from nixpkgs b7c2ada94fe99c15b0dbcf4d11fd7850b957a436, target `aarch64-unknown-linux-gnu`, and cross image `ghcr.io/cross-rs/aarch64-unknown-linux-gnu:0.2.5` digest `sha256:7f8308a8734d9fcd2ebbe9a3e4bdea74af293f0799d80c3cc341e340cda49a4c`, `cross test --target aarch64-unknown-linux-gnu -p nano-wasm-cache --lib -- --nocapture` compiled then executed under QEMU: 6 passed, 0 failed in 0.95s. This covers fresh compile vs serialized native-module reload, changed-Wasm/config misses, format bump and corrupt-entry miss on AArch64.

## 2026-08-16T05:58:08Z

AArch64 full vendored compiler corpus ran as a real aarch64-unknown-linux-gnu binary under pinned QEMU user 11.0.2 and cross image ghcr.io/cross-rs/aarch64-unknown-linux-gnu:0.2.5@sha256:7f8308a8734d9fcd2ebbe9a3e4bdea74af293f0799d80c3cc341e340cda49a4c. The exhaustive run finished in 12,753.22s with 1,490 passed, 4 failed and the same 2 intentional ignores as x86. Three failures are isolated-container fixture errors: the stacking tests could not open tests/contracts/boot-contracts/pox-4.clar because the manual runtime used the vendored workspace root while the test path is relative to the clar2wasm package directory; they require a correctly rooted rerun. One failure is a genuine AArch64 boundary: wasm_generator::tests::all_bindings_read_at_once_stays_loadable triggers Cranelift's AArch64 branch-range assertion at machinst/buffer.rs:1395 while the neighboring wide optional match, wide tuple, packed ABI and wide control/consensus tests load. This keeps the architecture acceptance open and pins the next isolated reproducer. Full log: /home/aldur/task130-clar2wasm-aarch64.log.

## 2026-08-16T08:13:28Z

2026-08-16: Closed the genuine AArch64 Cranelift branch-range boundary without patching Wasmtime. Upstream Wasmtime issue 12968 and fix 13392 show that one enormous AArch64 basic block cannot receive a constant island; the fix landed after the selected 36 LTS and is too large to backport safely. clar2wasm now emits bounded 512-entry structured blocks only for large list literals, preserving the old lowering for smaller lists and element evaluation/cost order everywhere. Final-source gates: x86 all_bindings_read_at_once_stays_loadable 1/1 in 148.98s; full sequences suite 115/115 in 9.30s; strict clar2wasm all-target Clippy green; actual aarch64-unknown-linux-gnu test binary under pinned QEMU 11.0.2 passed the formerly crashing exact test 1/1, exit 0, no OOM, in 3067.45s. The expensive emulated boundary timing is recorded; the full corrected AArch64 corpus remains open before Task 130 closure.

## 2026-08-16T11:54:00Z

2026-08-16: Corrected full AArch64 vendored compiler corpus is green from clean commit a080df73. The actual aarch64-unknown-linux-gnu test binary ran under pinned QEMU user 11.0.2 from the clar2wasm package directory with 8 test threads: 1,494 passed, 0 failed, 2 intentional ignores in 13,019.58s; Docker exit 0 and OOM false. This rerun clears all four prior failures: the bounded large-list blocks eliminate the Cranelift branch-range assertion, and all three stacking/PoX fixtures load from the corrected working directory. No interpreter fallback or alternate engine was used. AArch64 nano-vm and cross-architecture root/receipt/cost/refusal evidence remain open before Task 130 closure.

## 2026-08-16T11:59:20Z

2026-08-16: AArch64 node-VM corpus is green as an executed target binary, not merely cross-compiled. `cross test --target aarch64-unknown-linux-gnu -p nano-vm --lib --no-run` built from clean commit b70ed874 in 3m38s; pinned QEMU user 11.0.2 then ran the package-root test binary with 8 threads: 45 passed, 0 failed, 1 intentional ignore in 12.53s. Docker exit 0 and OOM false. This covers native-cache reload and invalidation, recorded-epoch compilation, module-load and contract refusal fail-closed, transaction rollback/roots, checkpoint state loading, and PoX-5 hydration on AArch64. Cross-architecture fixture/root/receipt/cost/refusal evidence remains open before Task 130 closure.

## 2026-08-16T12:25:35Z

2026-08-16: AArch64 checked-in capture scoreboard is green as an executed target binary. Rust 1.97.1 and cargo-cross 0.2.5 built target/aarch64-unknown-linux-gnu/debug/xtask from clean commit 6b5dd395. Because current openssl-sys requires OpenSSL 1.1+ while the pinned Xenial cross image ships 1.0.2, the disposable target image cross-compiled official OpenSSL 1.1.1w (published SHA-256 cf3098950cb4d853ad95c0841f1f9c6d3dc102dccfcacd521d93925208b76ac8) with the image toolchain; its AArch64 libraries require at most GLIBC_2.17, and the final xtask binary at most GLIBC_2.18. Pinned QEMU user 11.0.2 ran NANO_REPLAY_BOTH_ENGINES=1 xtask scoreboard: offline fixture integrity captured, state roots 340/340, receipts 340/340, all receipt cost dimensions 340/340, frozen mainnet receipt digests 500/500 over 8,702,046-8,702,592, replay depth 340/340, exit 0, no OOM. It truthfully reports no external NANO_MAINNET_STATE; the final-source x86 full-state inventory refresh remains open.

## 2026-08-16T12:32:51Z

2026-08-16: Final-source x86_64 vendored compiler corpus is green after the bounded large-list lowering change. From clean commit 193baa8e, CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command cargo test --manifest-path vendor/clarity-wasm/Cargo.toml -p clar2wasm --lib -- --nocapture completed 1,494 passed, 0 failed, 2 intentional ignores in 387.02s. This refreshes the complete x86 compiler/result/cost/ABI/module-load corpus on the same source already proven by the clean AArch64 full corpus.

## 2026-08-16T12:34:13Z

2026-08-16: Final-source x86_64 nano-vm corpus is green from clean commit cb5fed79. CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command cargo test -p nano-vm --lib -- --nocapture completed 45 passed, 0 failed, 1 intentional search-only ignore; build 58.78s and tests 1.24s. This refreshes native-cache equality/invalidation, recorded-epoch rebuilding/refusal, malformed-Wasm load refusal, rollback/roots, checkpoint loading, exact module inspection, and PoX-5 hydration after the final compiler change.

## 2026-08-16T12:36:04Z

2026-08-16: Final-source x86_64 checked-in capture scoreboard is green from clean commit b06f9eea with both engines enabled. CARGO_BUILD_JOBS=2 bash scripts/build-lock.sh nix develop --command env NANO_REPLAY_BOTH_ENGINES=1 cargo run -p xtask -- scoreboard exited 0: fixture integrity captured, state roots 340/340, receipts 340/340, all receipt cost dimensions 340/340, frozen mainnet receipt digests 500/500 over 8,702,046-8,702,592, replay depth 340/340. These rows are identical to the executed AArch64 scoreboard. It truthfully reports no external NANO_MAINNET_STATE; the final-source full-state contract inventory remains next.
