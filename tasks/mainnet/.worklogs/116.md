## 2026-08-11T23:15:15Z

2026-08-12: Started in isolated clone /home/aldur/task116-stacks-core.20260812 at surveyed head 70462db132; fetched origin/main and confirmed merge-base 6d58b498d with 37 branch-only commits. Located the manual marf_perfs benchmark and mmap/non-mmap tooling. Builds and timing runs are deliberately held until the isolated task037 replay stops, so the measurements do not contend with or perturb live replay evidence.

## 2026-08-11T23:22:29Z

2026-08-12: Static preflight found the branch's read benchmark unusable as checked in: it tests for an on-disk fixture but opens a new :memory: MARF, overwrites every option with MARFOpenOpts::default() (external blobs and mmap both false), and names block 0x00f0ffff although the generator's final block is 0x000ff001. Repaired equivalent isolated harnesses in local-only commits 26dfc0b27 (branch) and be2782402 (merge-base). They generate one external-blob fixture and open it read-only; the branch selects mmap on/off explicitly. No timing run has started while task037 owns the machine.

## 2026-08-11T23:29:28Z

Read-only benchmark audit found a second defect before measurement: both repaired readers called MarfConnection::get inside a cfg(test) binary. On the merge-base that helper performs a get-by-key plus get-by-hash consistency pair and then a third get-by-key; on the branch it performs get-by-key plus get-by-path. The apparent branch speedup would therefore include different test-only traversal counts. Both isolated harnesses now derive the same TrieHash from the generated key and call get_from_hash exactly once per timed lookup. Local experiment commits: branch dacef23e9 atop 26dfc0b27; merge-base 880008c77 atop be2782402. Diff-check is green; compilation and timing remain intentionally deferred while the isolated mainnet replay owns the machine.

## 2026-08-12T03:09:42Z

2026-08-12: A third pre-measurement audit found the timer still included generating, hex-encoding and hashing all 1,048,576 lookup paths. That identical CPU work would dilute the MARF speedup and make the result not specifically a read-path number. Both isolated harnesses now prepare TrieHash values before the timer and time exactly one get_from_hash per prepared path. Local-only commits: branch a9cb6815a atop dacef23e9; merge-base fc87c23af atop 880008c77. Rustfmt and diff-check are green; no build or timing run overlapped the live replay.

## 2026-08-12T03:54:46Z

2026-08-12: First compile exposed two harness/environment facts before timing. The repaired generator still needs the public ClarityMarfTrieId trait for BlockHeaderHash::sentinel; fixed in local-only commits branch 22ebfe435/base f03b2b9ae. More importantly, nano's shell Rust 1.97.1 rejects a branch-only TransactionConnection lifetime that the repository pins Rust 1.96.0 for; patching branch production code or comparing different compilers would invalidate the experiment. Installed and verified pinned rustc/cargo 1.96.0 inside the Nix dependency environment. The upstream release profile's fat LTO grew rustc past 10 GiB RSS and left only 1.6 GiB available beside the retained rollover node, so that exact build was interrupted. Both variants are now built identically with opt-level 3, thin LTO, one codegen unit, debug=0, one Cargo job, separate rust196 targets. The shared external-blob fixture passed all 1,048,576 generator read-backs in 63.83s; files are a 128 KiB index plus 501,740,804-byte blob.

## 2026-08-12T04:15:54Z

2026-08-12: Do not reuse/reflink Cargo targets between the merge-base and branch. A copied pinned-compiler base target let Cargo reuse local clarity-types output while branch clarity expected branch-only APIs, proving that worktree isolation without independent target construction is insufficient for this comparison. That copied target is excluded from all evidence. Branch is rebuilding from an empty, private task116-core-branch-rust196-fresh-target with the identical pinned compiler/profile; base remains its independently built rust196 target. The MARF fork also carried an unrelated upstream eval-hook signature (65dac1334) that does not compile at this branch head; local compatibility commit 0c9bd82d5 restores exactly the merge-base callback signature in trait+impl, outside the measured MARF path.

## 2026-08-12T04:36:22Z

2026-08-12: Measured the fork's read path on pinned Rust 1.96.0, opt-level3/thin-LTO/codegen-units1/debug0, one job, replay stopped, same immutable external-blob fixture (index SHA a3b00762..., blob SHA 73a2c253..., 1,048,576 prepared hashes, one get_from_hash each). After one warm-up per variant, five position-balanced alternating rounds yielded ns/read: base 15,828/16,026/16,521/15,545/16,386 (median 16,026); branch pread 7,539/7,892/7,179/7,377/7,489 (median 7,489, 2.14x base); branch mmap 6,074/6,412/6,262/6,232/6,413 (median 6,262, 2.56x base and 1.20x branch pread). Fixture hashes/inodes/sizes/mtimes were unchanged. Full run log SHA-256 a662ea64cde7465dca89e2390c0dba8d5256692884181d69055879c0acf963be. This proves the fork's refactor matters substantially, while mmap itself contributes the last ~20%; reduced long-MARF safety tests remain before checking the task bullet.

## 2026-08-12T05:04:00Z

The branch's reduced long-MARF safety matrix passed 35/35 in 368.98 s on the independently built branch binary. It covered the 128/128 and 15,500/10 cases across immediate/deferred, batch, compression and mmap variants, plus patch expansion; zero failures or ignores. Together with the immutable-fixture timings above, this completes the branch-side measurement task without relying on the fork author's claimed speedup.
