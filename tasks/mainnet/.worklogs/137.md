## 2026-08-17T07:58:30Z

Started from clean main at d56b0fe2. Repository audit found no fuzz crate, persistent corpus, sanitizer/Miri job, mutation gate or cross-architecture consensus-output comparison. Existing deterministic tests are valuable regressions but do not satisfy continuous adversarial search. The first atomic slice will add reusable structure-aware stable harnesses plus owned checked-in seed corpora and a bounded ordinary-CI replay; long-running fuzz engines, sanitizer jobs, fault injection, chaos, architecture comparison and mutation evidence remain explicitly open.

## 2026-08-17T10:40:17Z

First bounded corpus checkpoint implemented in crates/nano-adversarial. Six stable harnesses cover P2P framing/protocol admission, transaction+Nakamoto block codecs, signer+StackerDB codecs, joint checkpoint/bundle manifests, structured MARF operations, and accepted Clarity source through production compile/load. Each target owns checked-in seeds; the smoke runner enforces 32 seeds/4 MiB per target and requires successful-path coverage bits. scripts/ci.sh and gates.yml run adversarial-smoke on every change. Evidence: cargo check -p nano-adversarial --all-targets GREEN; development corpus smoke 1/1 GREEN; cargo clippy -p nano-adversarial --all-targets -- -D warnings GREEN; CI-profile scripts/ci.sh adversarial-smoke 1/1 GREEN; workflow lint, formatting, taskmd validate 136/136, and git diff --check GREEN. This is an intermediate checkpoint only: no Task 137 checkbox is claimed until the actual structure-aware fuzz jobs, differential oracle, continuous artifact retention, sanitizers, fault/chaos, cross-architecture and mutation gates exist.

## 2026-08-17T11:06:48Z

Added a separate cargo-fuzz workspace so libFuzzer dependencies stay out of the production workspace and lockfile. Six structured frontends derive bounded P2P frames, seed edits, MARF operation streams and accepted Clarity programs before calling the stable harness crate. The pinned Nix shell now supplies cargo-fuzz 0.13.2. Evidence: all six targets built together with `--sanitizer none`; disposable-corpus runs completed for P2P 100/100, codecs 100/100, signer/StackerDB 100/100, checkpoint 100/100, MARF 100/100 and Clarity 20/20; strict all-target fuzz-workspace Clippy passed with `-D warnings`. Checked-in corpora were never mutated and temporary corpora were removed. This remains an intermediate checkpoint: the first checkbox still includes P2P session state and full checkpoint import, and continuous jobs, differential results, sanitizers, fault/chaos, architecture comparison and mutation evidence are still open.

## 2026-08-17T12:02:15Z

Added the continuous fuzz/debug runner and scheduled six-target workflow. Ordinary CI now builds all structured frontends and executes 20 cases per target; scheduled and manual runs default to 900 seconds per target. Runs use disposable corpora seeded from the owned canonical inputs plus any checked-in raw regression. On a reproducible failure the runner invokes cargo-fuzz minimization, writes a content-addressed input under `fuzz/corpus/<target>`, and the workflow pushes it to a unique durable branch and opens a pull request; the 30-day artifact is only a convenience copy. Evidence: actionlint and shellcheck GREEN; formatting and diff-check GREEN; exact `scripts/ci.sh fuzz-smoke` GREEN for all six targets after a cold 6m49s build; an isolated intentional-panic probe produced, minimized, content-addressed and then cleaned a retained input while leaving the checked-in corpus untouched. The task remains unchecked until the scheduled workflow has actually run and the still-missing session/import, differential, sanitizer, fault/chaos, architecture and mutation scopes are implemented.

## 2026-08-17T12:28:28Z

Added the first pinned stacks-core differential target. Nano and the reference implementation independently decode transaction and Nakamoto block inputs, compare acceptance, consumed length and canonical encoded bytes, and retain any mismatch through the existing workflow. The owned transaction/block corpus reaches both successful paths. Evidence: canonical differential corpus 3/3 GREEN; strict `nano-adversarial` all-target Clippy GREEN; structured differential fuzz 100/100 GREEN with 1,327,367 counters; strict nested fuzz-workspace all-target Clippy GREEN; full seven-target `scripts/ci.sh fuzz-smoke` GREEN. This advances but does not complete the differential checkbox: roots, receipts, costs, events and refusal-class differentials still need structure-aware generation and retained continuous evidence.

## 2026-08-17T12:47:52Z

Added a real authenticated P2P session-state fuzz target. The stable harness uses a bounded current-thread Tokio runtime and a loopback nano-p2p Listener/Session pair, completes the production handshake, then exercises ping, neighbor discovery, known inventory, unknown-inventory NACK and advertised-view update paths while asserting peer identity and exact answered/nacked/ignored accounting. A structured cargo-fuzz frontend caps each conversation at 32 operations; canonical and persistent raw corpora, per-change smoke and scheduled workflow ownership are wired for the eighth target. Evidence: canonical bounded corpus 1/1 GREEN; root nano-adversarial strict all-target Clippy GREEN; structured target 20/20 GREEN at 1,332,686 instrumentation counters and 112 MiB RSS; full eight-target scripts/ci.sh fuzz-smoke GREEN; nested fuzz-workspace strict all-target Clippy GREEN. The first task checkbox remains open because full checkpoint-import fuzzing is still missing.

## 2026-08-17T13:10:10Z

Completed the structure-aware target inventory with a full checkpoint-import fuzzer. Each bounded case builds a valid stacks-core external-blob MARF with up to four blocks and sixteen deduplicated writes per block, including ancestry/back-pointers and overwrites; nano imports the SQLite/blob graph and must match the published root and every final value. Added a structured frontend, canonical two-block overwrite corpus, persistent raw corpus, per-change target and scheduled target. Evidence: canonical nine-target corpus replay 1/1 GREEN; nano-adversarial strict all-target Clippy GREEN; checkpoint_import structured fuzz 20/20 GREEN with 1,336,975 counters and 112 MiB RSS; full nine-target scripts/ci.sh fuzz-smoke GREEN; nested fuzz-workspace strict all-target Clippy GREEN. Task 137's first checkbox is now complete; all other task items remain open.

## 2026-08-17T14:05:17Z

Expanded pinned differentials to compare every generated checkpoint block root plus bounded Clarity compiler/interpreter results, all five cost dimensions, canonical ordered print events, and six typed runtime refusal classes. The full 11-target fuzz smoke is green; the result/cost/event target completed 20/20 cases at about 39,716 feature edges and 134 MiB RSS, and the refusal target completed 20/20 at about 35,296 edges and 119 MiB RSS. Canonical corpus replay, root and fuzz-workspace strict Clippy, formatting, actionlint, shellcheck, taskmd validation, and diff-check are green. This advances but does not close the differential checkbox: stateful writes, AssetMap, and the full receipt envelope still need comparison.

## 2026-08-17T14:28:40Z

Completed the consensus-visible differential surface. The new bounded stateful target deploys the same generated contract through clarity-wasm and the pinned stacks-core interpreter, then compares the deployment receipt, public-call outcome, value, all five costs, exact AssetMap, ordered events, post-call state read and sealed MARF root. Six owned cases cover data vars, maps, ordered multi-writes, FT and NFT effects, and error-response rollback. Its first corpus replay found and pinned an oracle-wrapper bug that mislabeled a rolled-back error response as Success despite identical receipt/state; the wrapper now reports AbortedByResponse. Evidence: deterministic all-corpus replay GREEN; root nano-adversarial+nano-oracle strict Clippy GREEN; structured stateful target 20/20 GREEN at about 39,704 feature edges and 130 MiB RSS; fuzz-workspace strict all-target Clippy GREEN; scoped formatting, actionlint, shellcheck, taskmd validation and diff-check GREEN. The immediately preceding 11-target matrix was green at 935b8feb, and the new twelfth target is wired into per-change and continuous CI with durable finding retention. The differential checkbox is complete; sanitizer, fault, chaos, architecture and mutation work remains open.

## 2026-08-17T15:02:42Z

Added a pinned minimal Fenix nightly sanitizer shell and a four-mode push/PR/manual workflow. Miri runs nano-primitives with isolated Proptest persistence disabled (4/4 green); ASan runs the Wasmtime cache's single unsafe deserialize boundary (6/6 green); Clang UBSan instruments bundled secp256k1 and SQLite dependencies (nano-crypto 1/1 and nano-marf 20/20 green); TSan rebuilds std with matching instrumentation and runs all nano-queue tests, including a new four-thread exact-capacity regression (9/9 green). Stable nano-queue tests and strict Clippy, flake evaluation, rustfmt, ShellCheck, Actionlint and scoped diff-check are green. Rustc does not expose undefined as a Rust sanitizer, so Rust UB is covered by Miri and the undefined mode is explicitly limited to the C dependency boundary. Fault injection, network chaos, cross-architecture comparison, mutation testing, a real scheduled continuous run and release-inventory run status remain open.

## 2026-08-17T15:13:30Z

Added an intermediate native cross-architecture gate without claiming completion. A canonical Epoch 4 report now executes the six stateful compiler/interpreter cases and six typed refusal cases and emits deterministic JSON containing result bytes/reprs, all five costs, exact AssetMap, ordered events, sealed roots and refusal text. Two independent x86-64 runs were byte-identical at 17,712 bytes and SHA-256 12074af47ff96f29f721735f9ebc6547a303be4e5da312b60b604367d22c3a86; the deterministic 12-target corpus smoke and strict nano-adversarial Clippy are green. The workflow runs this report natively on the current ubuntu-24.04 x86-64 and ubuntu-24.04-arm AArch64 hosted runners, checks the pinned digest on each, and directly compares the two artifacts. The checkbox remains open until a real AArch64 workflow run is retained; broader executable block-corpus coverage and release-inventory run status also remain open.

## 2026-08-17T15:40:17Z

Completed the mutation-testing decision gate with pinned cargo-mutants 27.1.0 in the Nix development shell and a push/PR/manual workflow whose actions use the latest releases by full commit SHA. The isolated campaign caught the unconditional authentication-success mutant 1/1, all selected Epoch 4 fork-choice mutants 8/8, and the fake-success MARF seal mutant 1/1; the packaged scripts/mutations.sh rerun completed the same three campaigns green. The first fork-choice run exposed that deleting snapshot_for's Bitcoin timestamp was not observed, so a focused public-API regression was added and the selector campaign then killed all eight mutants. Evidence: nano-sortition decisions 1/1 GREEN; strict nano-sortition all-target Clippy GREEN; ShellCheck, Actionlint, flake evaluation, taskmd validation 136/136 and git diff-check GREEN. Fault injection, network chaos, real scheduled fuzz evidence, native AArch64 evidence and release-inventory last-run reporting remain open.

## 2026-08-17T15:51:37Z

Completed the deterministic network-chaos gate and wired it into every-change CI. The bounded matrix covers fragmented-byte deadline abuse (1/1), a peer tip ahead of a delayed local Bitcoin view (1/1), signed-chain equivocation, substitution, partition and peer removal (4/4), replication peer loss/rate limits/errors/rubbish/forged chunks/pool growth (8/8), deterministic tenure throttling, a tip moving mid-round, inventory partitioning, and a Bitcoin reorganization. Added the missing reward-cycle restart witness: the captured suffix crosses the actual cycle boundary, closes in three-block chunks, reopens after every chunk, and matches the uninterrupted tip, header/content root, ledger/accounting and tenure state. The packaged scripts/network-chaos.sh gate is GREEN end to end; the new exact boundary test is GREEN independently; strict nano-conformance CI-profile Clippy, rustfmt, ShellCheck, Actionlint, full-SHA action policy and diff-check are GREEN. Fault injection, native AArch64 evidence, scheduled fuzz evidence and release-inventory last-run reporting remain open.

## 2026-08-17T16:24:53Z

2026-08-17: Added the first deterministic storage-fault slice with pinned libfiu 1.2 in the Nix shell and a mandatory CI/release-inventory gate. After one clean block, the gate injects EIO and ENOSPC writes, EIO reads, short reads/writes, and fsync failure through the real POSIX/SQLite/MARF path; it also runs an actual read-only state directory. Every case reopens and replays to the uninterrupted tip, content root, accounting, executed ledger, parent-tenure proof and tenure start. The gate exposed and fixed a false consensus verdict: VM/host failures were being collapsed into InvalidTransaction; they now retain txid/height as typed TransactionExecution. Exact storage gate, conditional inventory 6/6, and strict nano-chainstate+nano-conformance all-target Clippy are green. Corrupt/torn/truncated files and exhaustive final commit-point coverage remain open, so the task checkbox stays unchecked.

## 2026-08-17T16:32:47Z

Extended the deterministic storage gate with corrupt first pages and half-file truncations for both MARF and Clarity stores, plus both cross-file generation-tear directions. A MARF one block ahead initially exposed a conformance reopen bug: it fell back to checkpoint accounting when the sealed tip had no matching ledger. The helper now follows production recovery to the deepest ancestor with a committed ledger, discards the incomplete seal, and replays from the complete parent; an ahead Clarity side store remains unreachable until its matching MARF seal. Both cases converge to the uninterrupted tip, root, accounting, executed suffix, tenure proof and tenure start. The full two-test storage gate, strict nano-conformance all-target Clippy, rustfmt, ShellCheck, Actionlint, action pinning, flake evaluation, release inventory 6/6, taskmd 136/136 and diff-check are green. Rename injection and exhaustive commit-point coverage remain open, so the fault-injection checkbox stays unchecked.

## 2026-08-17T16:54:40Z

Completed deterministic durability-boundary injection. A small libfiu callback helper selects the nth occurrence, so one captured-block commit is rerun while each observed durability syscall fails independently. The gate exercised all 9 fsync calls; the same path contained 0 fdatasync and 0 rename calls. Every injected run avoided a false consensus-invalid verdict and resumed to the exact clean tip, root, accounting, executed suffix, parent-tenure proof and tenure start. The full 3-test storage gate, release inventory 6/6 and strict nano-conformance all-target Clippy are green. Together with EIO, ENOSPC, short I/O, read-only state, corrupt/truncated files, cross-file generation tears and the existing process-kill matrix, the storage-fault checkbox is complete.

## 2026-08-17T16:59:52Z

Closed the remaining non-block rename surface in the mandatory storage gate. SortitionTracker's private writer boundary is now exercised at each of its four ordered file replacements (history, leader keys, waterfall payouts, snapshot): failure before the first preserves the prior complete generation; failures after an earlier replacement make the mixed generation fail the production loader and fall back to the authenticated capture. The helper remains private and the production path still performs the same write-to-partial then rename. Evidence: exact rename matrix GREEN; full storage gate GREEN (four tests including all 9 observed block-commit fsyncs); nano-follower unit suite 69/69 GREEN; strict nano-follower+nano-conformance all-target Clippy GREEN; ShellCheck, taskmd 136/136 and diff-check GREEN.

## 2026-08-17T17:04:34Z

Added fail-closed adversarial run history to release qualification without claiming hosted evidence. adversarial-jobs.json owns five mandatory workflow groups: bounded gates, continuous fuzzing, sanitizers, cross-architecture comparison and mutation decisions. The qualification workflow collects each latest completed run and last successful run through gh; release-report prints run IDs, timestamps, revisions and URLs, and rejects missing, failed or wrong-revision evidence. Offline reports enumerate every job and owner as UNEXECUTED. Parser/evaluator unit tests, strict xtask all-target Clippy, a deterministic fake-gh collector probe, ShellCheck, Actionlint, full-SHA action policy, flake evaluation, taskmd validation and diff-check are green. The continuous-fuzz and native-AArch64 boxes remain open because this checkout has no authenticated GitHub session and no real hosted run is being claimed.

## 2026-08-17T17:10:07Z

Corrected the hosted-run inventory after detecting a circular qualification dependency: gates.yml cannot require its own currently running revision to have already completed successfully. The bounded deterministic adversarial suite remains a direct mandatory gate; adversarial-jobs.json now tracks only the four independent hosted workflows whose prior successful runs can legitimately be audited.

## 2026-08-17T17:37:48Z

Read-only hosted-state audit on 2026-08-17: local main ff0fa1b8 is 232 commits ahead of origin/main 3335643d. The public GitHub API reports 25 workflow runs, all the legacy gates workflow; latest run 31803980644 at 3335643d completed failure on 2026-08-14, and no continuous-fuzz, sanitizer, mutation, or native-AArch64 run exists for the current implementation. The Nix shell provides gh 2.97.0 but this host has no GitHub authentication. No push or workflow dispatch was attempted; hosted and AArch64 checkboxes remain open rather than substituting local evidence.

## 2026-08-17T17:51:49Z

Hosted evidence on exact main revision 4e385847: runtime sanitizer run 32051262913 completed SUCCESS in 9m30s with all four native jobs green (Miri, address, undefined-behavior C boundary and thread sanitizer). Cross-architecture run 32051262619 completed SUCCESS in 9m28s: native ubuntu-24.04 x86_64 and ubuntu-24.04-arm AArch64 both executed and digest-checked the full Epoch 4 report, uploaded separate artifacts, and the mandatory compare job completed successfully in 6s, proving every emitted consensus-visible byte identical. Evidence: https://github.com/adriano-stacks/nano-stacks/actions/runs/32051262913 and https://github.com/adriano-stacks/nano-stacks/actions/runs/32051262619. The architecture checkbox is now complete. Task 137 remains in progress because mutation/gates are still running and no scheduled continuous-fuzz run exists yet.

## 2026-08-17T22:27:18Z

Enforced the repository policy mechanically: scripts/check-actions-pinned.sh now requires each external uses reference to carry a full 40-character SHA and exact release-tag comment, resolves GitHub's current latest non-draft release, and verifies that tag peels to the pinned commit. Verified on 2026-08-17 against checkout v7.0.1, upload-artifact v7.0.1, download-artifact v8.0.1, rust-cache v2.9.2, and install-nix-action v31.11.1; Nix pin check, ShellCheck, actionlint, taskmd validate, and diff check were green.

## 2026-08-17T22:31:11Z

Pre-schedule retention audit found and fixed a real gap: the failure step used git diff --quiet, which ignores newly created untracked finding-* files and could leave a crash only in an expiring artifact. The workflow now uses git status --porcelain for the target corpus, so tracked mutations and new minimized findings both trigger the durable branch/PR path. Verified untracked-file detection, Nix workflow gate (including latest/full-SHA action resolution), actionlint, ShellCheck, taskmd validation and diff check.

## 2026-08-17T22:42:14Z

Bound adversarial evidence to the required GitHub trigger event (schema 2): continuous fuzz accepts only schedule runs; sanitizer, cross-architecture, and mutation evidence accepts only push runs. The collector filters by event and preserves it in every run record; release evaluation rejects mismatched/manual events. Focused adversarial_runs tests: 3 passed, including workflow_dispatch refusal. Strict cargo clippy -p xtask --all-targets -- -D warnings: green. shellcheck, actionlint, latest full-SHA action-pin verification, jq, rustfmt, diff-check, and taskmd validation: green. Cleared 2.0 GiB of stale nano-mempool/nano-sync target artifacts after the clean Nix release build proved a local fingerprint mismatch.

## 2026-08-17T22:51:16Z

Made hosted evidence self-describing and quota-independent before the scheduled run. Release-report now prints each mandatory event (for example continuous-fuzz via schedule). The exact no-gates integration regression passed from a clean commit. The latest-action verifier now follows GitHub canonical releases/latest redirects instead of consuming unauthenticated API quota, then still peels each tag with git ls-remote and requires the configured full SHA. ShellCheck, actionlint, strict xtask Clippy, formatting, diff-check, taskmd validation, and all current latest/full-SHA resolutions are green.

## 2026-08-18T12:30:18Z

2026-08-18 handoff. The scheduled failures and isolated reproductions are from
exact revision 3018ac4aa72c4594c08fd75b0ffd932acae18685. During the handoff,
the separate Task145 owner advanced main to c03bde7b; the Task137 changes below
remain unstaged on top of that commit.

Scope/status:
- Task137 remains in-progress. The first genuine scheduled run was GitHub Actions run 32094317716 on exact revision 3018ac4a. Ten of twelve matrix jobs passed. clarity_stateful_receipt_differential job 95582550417 and transaction_and_block_differential job 95582550589 failed.
- Both failure jobs uploaded artifacts successfully, but the durable branch/PR retention step failed immediately. No refs/heads/fuzz-finding/* exist remotely. Exact step logs require GitHub authentication; this host has no GH_TOKEN/GITHUB_TOKEN and gh is not logged in. Run/job metadata is preserved at /home/aldur/task137-scheduled-jobs-32094317716.json and /home/aldur/task137-hosted-watch.jsonl. Logged-out job HTML is /home/aldur/task137-job-95582550417.html and /home/aldur/task137-job-95582550589.html. Do not claim a retention cause without the step log. The workflow is .github/workflows/fuzz.yml. Every uses reference is already a latest released action pinned to a full 40-character SHA.

Finding 1, transaction/block differential:
- Minimized retained input: fuzz/corpus/transaction_and_block_differential/finding-4326b1f21c96c482f3d7ae126e374255bf110d2f1ee54ea355549fbc153166ba, 9 bytes, base64 MDE4bTYBAAAw.
- Root cause is proven: nano preserved arbitrary transaction-version bytes through decode/re-encode; pinned stacks-core canonicalizes solely from bit 7, Mainnet when clear and Testnet when set.
- Unstaged fix currently edits crates/nano-codec/src/lib.rs and crates/nano-mempool/src/lib.rs: remove TransactionVersion::Other, parse by bit 7, encode as 0 or 0x80, and test all 256 bytes. origin_address keeps its Option API but is now always Some; sponsor_address remains optional.
- Exact unit gate passed: bash scripts/build-lock.sh cargo test -p nano-codec tests::transaction_version_uses_only_the_network_bit -- --exact --nocapture.
- Exact minimized fuzz replay passed after the fix using cargo fuzz run with -runs=1.
- Still required before commit: strict Clippy and package tests for nano-codec plus nano-mempool. fuzz/Cargo.lock has an unstaged mechanical update adding hex and serde_json to nano-adversarial dependencies; audit and include it if retained by a clean fuzz build.

Finding 2, stateful receipt differential:
- Reproduced from a detached exact-HEAD worktree /tmp/nano-task137-head-3018ac4a with isolated target /tmp/nano-task137-fuzz-target.
- Exact command:
  bash /tmp/nano-task137-head-3018ac4a/scripts/build-lock.sh env CARGO_TARGET_DIR=/tmp/nano-task137-fuzz-target NANO_FUZZ_TMP=/tmp NANO_FUZZ_SANITIZER=none /tmp/nano-task137-head-3018ac4a/scripts/fuzz.sh clarity_stateful_receipt_differential 900
- Minimized input is one byte 0x31, SHA-256 6b86b273ff34fce19d6b804eff5a3f5747ada4eaa22f1d49c01e52ddb7875b4b. It is retained in main at fuzz/corpus/clarity_stateful_receipt_differential/finding-6b86b273ff34fce19d6b804eff5a3f5747ada4eaa22f1d49c01e52ddb7875b4b.
- It selects STATEFUL_MAP with amount=1, key=0, empty note, replace=false, therefore map-insert.
- The observations match in deployment, result, AssetMap, ordered print event, snapshot, read costs, runtime, write count, and sealed root. Only call write_length differs: compiled 57 versus pinned interpreter 58.
- Immediate code seam: vendor/clarity-wasm/clar2wasm/src/words/maps.rs computes the post-2.05 SetEntry/InsertEntry cost guest-side as serialization_size(key) + serialization_size(value) + 1 optional tag. The interpreter instead charges ClarityDatabase::set_entry returned serialized_byte_len from clarity_db.rs special_set_entry_v205. For this exact value, the guest formula yields 17 + 39 + 1 = 57 while the database reports 58. Do not patch a constant; first add a focused cost/result crosscheck for map-insert/map-set of uint -> {amount:uint,note:(buff 32)} with an empty note, then determine which database serialization/sanitization component supplies the missing byte. Prefer using the authoritative host returned serialized_byte_len if a general ABI change is needed.
- The repeated no-analysis lines during fuzzing are diagnostic noise from the reference wrapper and not the asserted failure.

Worktree ownership at handoff:
- Our unstaged files: crates/nano-codec/src/lib.rs, crates/nano-mempool/src/lib.rs, fuzz/Cargo.lock, both finding-* corpus files, and this Task137 worklog.
- An unrelated external agent owns the staged Task145 batch: tasks/mainnet/145-resolve-clarity-native-name-activation-collisions.md, tasks/mainnet/.worklogs/145.md, vendor/clarity-wasm/clar2wasm/src/wasm_generator.rs, and vendor/clarity-wasm/clar2wasm/src/words/functions.rs. Do not absorb, reset, restage, or commit those files with Task137. Recheck git status because that agent may commit them after this handoff.
- Cargo must be run through bash scripts/build-lock.sh and shared-target workloads must remain serialized. Another Codex session under parent PID 18694 has repeatedly launched Cargo outside the lock; preflight pgrep -x cargo and pgrep -x rustc. The exact fuzz target is safely isolated in /tmp and fully built.
- The live nodes on 127.0.0.1:20492 and :20493 must not be stopped or mutated.

Next sequence:
1. Add the focused map write-length regression and make it fail at 57 vs 58.
2. Fix the general map write-size seam; replay the one-byte stateful corpus and run affected map/cost suites plus strict clar2wasm Clippy.
3. Finish codec/mempool strict Clippy and tests; replay the 9-byte codec corpus.
4. Commit small atomic batches without staged Task145 content. First codec canonicalization + its finding; second map write-cost fix + stateful finding; workflow retention repair separately once the exact CI failure is known or a credential-independent durable design is proven.
5. Run the full 12-target deterministic fuzz smoke. Task137 must remain open until a later genuine scheduled run is 12/12 green and durable retention is demonstrated; workflow_dispatch is not substitute evidence.

Host/follower note:
- After Btrfs delayed reclaim, df reports about 302 GiB free on /home and 45 GiB on /tmp, so Task135 fresh-import free-space floor is satisfied. The prior interrupted state remains preserved as /home/aldur/follower-import-3018ac4a/state-interrupted-vm-upgrade-20260818-085248 and the new state directory is empty. The /home/aldur/mainnet-follower-3018ac4a artifact symlink is currently missing and must be rebuilt from exact clean revision 3018ac4a before restarting /home/aldur/follower-import-3018ac4a/run.sh. Do not use a dirty Task145 artifact.

## 2026-08-18T13:23:28Z

Fixed both genuine scheduled-fuzz divergences on main. cd40e16d makes map-set and map-insert charge the authoritative database serialized length through the Wasm host ABI; its focused release regression, exact one-byte retained replay, 17 map tests, 46 cost crosschecks, 4 Wasm map-generation tests, and release all-target clar2wasm Clippy passed. b7268240 canonicalizes every transaction version byte by network bit 7 like pinned stacks-core; nano-codec 4/4, nano-mempool 15/15, strict all-target Clippy, and the exact nine-byte retained replay passed. Task 137 remains open until a genuine scheduled run is 12/12 green and durable retention succeeds.

## 2026-08-18T13:38:20Z

Full post-fix deterministic fuzz smoke passed 12/12 on exact main bad1e190 after both retained findings were committed. scripts/ci.sh fuzz-smoke built every structured target and completed 20 cases each, including transaction_and_block_differential with the nine-byte corpus and clarity_stateful_receipt_differential with the one-byte corpus. The task remains in-progress because local smoke is not scheduled-run evidence and durable remote retention still needs a genuine successful schedule event.

## 2026-08-18T14:00:39Z

Redesigned scheduled-fuzz durable retention without claiming the unseen step log. The single opaque retention step failed in under one second on run 32094317716 with no remote branch created; job metadata cannot name the failing command and logs require admin credentials. The workflow now splits retention into three steps whose public step conclusions localize any future failure: commit the finding to a durable branch, push it with an explicit x-access-token authorization header independent of checkout-persisted credentials, and open the pull request. A local probe replayed the exact step commands from a detached shallow clone against a bare origin: the durable branch received the finding commit, outputs propagated, and the clean-scope guard reported retained=false. actionlint, shellcheck, latest full-SHA action-pin verification, formatting, taskmd validation 138/138 and diff-check are green. Scheduled 12/12 evidence and a demonstrated remote retention still require the next genuine schedule event.

## 2026-08-19T03:54:44Z

Scheduled run 32211140772 (2026-08-19T03:09Z, revision 07943512) delivered the first hosted proof of durable retention: both failing targets pushed fuzz-finding branches through the redesigned commit/push steps, and public step metadata isolated the only remaining failure to gh pr create. Both retained findings decode to one root cause: nano cached raw wire bytes and hashed them as txid, while pinned stacks-core hashes the canonical re-serialization. A transaction whose version byte is non-canonical (finding sets byte 355 of the block seed, the embedded transaction version, 0x80 to 0xd4) therefore got the wrong txid, failed nano's decode-time merkle check that the reference also performs but with canonical txids, and flipped block acceptance; the codecs round-trip target tripped on the same canonicalization at re-encode. Fix: Transaction::decode canonicalizes the cached bytes' version byte, so txid, as_bytes and encode agree with the reference; the round-trip harness now requires re-encoded bytes identical to the wire except where a differing byte is exactly the version canonicalization wire&0x80. Evidence: new nano-codec regression over three non-canonical version bytes GREEN; nano-codec, nano-chainstate, nano-mempool suites GREEN; strict release workspace Clippy GREEN; both retained findings replayed GREEN via cargo fuzz -runs=1.
