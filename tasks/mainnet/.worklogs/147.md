## 2026-08-23T08:46:53Z

2026-08-23, from the session that closed task 146. This is a hand-off, not a
status change: the task's own tests are still uncommitted in the shared tree and
they are yours.

**The refusal is fixed** (26067420, in clarity-wasm, which is where my session's
work has been; none of your files were touched or committed).

The trait is what made the offending value, not what broke it, which is why the
reduced trait probes all agree. Duck-typing an *optional* has to project its
payload's slots to the target's shape on both branches, so the `none` branch
offers a tuple whose principals are the unmaterialised `(0, 0)`. Nano tried to
materialise that into the runtime-shape arena and refused the whole transaction.
A shape that cannot be read is a shape that cannot be preserved, and the answer
for "no shape" is the same zero handle a value that never crossed the host
carries.

The smallest reproduction needs no trait at all — a `map-set` whose value holds
an absent optional of a tuple of principals:

    (define-data-var tail (optional {a: principal, b: principal}) none)
    (define-map links {a: principal, b: principal}
      {prev: (optional {a: principal, b: principal}),
       next: (optional {a: principal, b: principal})})
    (define-public (poke (s principal))
      (let ((key {a: s, b: s}) (old (var-get tail)))
        (map-set links key {prev: old, next: none})
        (var-set tail (some key))
        (ok u2)))

Bisected from your `MARKET` snippet: `(append-registration key)` was the
trigger, and inside it neither the `match` nor the `merge` was needed. Pinned in
`clar2wasm`'s `borrowed_operand_charges` with both the absent and the present
payload, so the fix cannot become "never remember a payload's shape", and
verified to fail without it.

**Your reproduction passes now.** `a_clarity_four_trait_principal_survives_the_registration_shape`
is green.

**Two operational facts worth having.** Both nano nodes are halted at the same
place: port 20492 sits at 8,815,025 and the witness's receipt captures stop at
the same height, so nothing has executed past this since it appeared.

And the last two items cannot be finished with the current binary, for a reason
that is not this bug. The deployed state predates the consensus-profile record,
so a binary carrying any newer compiler identity refuses it with
`MissingProfile` — I confirmed that against a reflink copy rather than the live
state. Replaying 8,815,026 at its exact prestate, and restarting the node to
watch it advance, therefore both wait on the same attested-bundle ceremony that
tasks 106 and 053 wait on. Overriding the profile on the live state would forge
the very binding the design exists to prevent.

Also unbooked: `the_mainnet_8815026_trait_registration_executes_at_its_exact_prestate`
is ignored but not named in `ignored-tests.toml`, so the `tests` gate fails while
your files are in the tree. That is the one thing standing between your working
copy and a clean gate.
