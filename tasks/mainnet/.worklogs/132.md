## 2026-08-16T21:43:02Z

Started the literal ingress-budget audit on clean main at 719bb99b. Confirmed four externally fed unbounded node channels (blocks, proposals, chunks, transactions); observer delivery has a bespoke byte counter around an unbounded channel; P2P sessions retain 256 pushes by count after allowing ~16 MiB frames; relay queues retain 1,024 variable-sized blocks/transactions by count; and the RPC router has no route-specific concurrency, rate or slow-read policy. First implementation slice will introduce one byte-accounted queue primitive and migrate the four RPC-to-role channels without weakening admission semantics. Live follower and its state remain untouched; Cargo stays serialized.

## 2026-08-16T21:47:39Z

Added the shared nano-queue primitive as the first isolated implementation slice. It enforces independent item/byte limits, supports reservation before irreversible admission, never waits for capacity, and reports live items/bytes/oldest age plus cumulative drops/saturations. It recovers capacity on receive or abandoned permits and clears accounting when its consumer stops. Focused tests 5/5 and strict all-target Clippy are green; no RPC behavior changed in this commit.

## 2026-08-16T22:05:38Z

Migrated all four RPC-to-role hand-offs from Tokio unbounded channels to nano-queue: block uploads (8/16 MiB), proposal validation (2/4 MiB), StackerDB replication (256/16 MiB) and transaction relay (256/16 MiB). Transactions reserve before mempool mutation; chunks reserve before replica/event mutation; blocks and proposals return explicit 503 + Retry-After before acknowledging work. Saturation/recovery HTTP tests are 4/4 and prove no mempool/chunk/block/proposal state changes while full. Full nano-rpc is 45 passed with only the existing live-Hacknet infrastructure ignore; nano-node is 88/88; targeted all-target check and strict Clippy are green. The observer dispatcher and P2P count-only queues remain open next slices.

## 2026-08-16T22:12:55Z

Replaced the event observer's bespoke byte-counter around an unbounded channel with nano-queue. Each observer is bounded at 1,024 items and 32 MiB; a receive lease keeps bytes/age charged through the HTTP attempt, and /nano/sync_status now reports queued bytes, oldest age and saturation count alongside delivered/dropped/undelivered. Byte and count saturation preserve ordered sequence gaps and recover without restart. Queue tests 6/6, observer tests 7/7, full nano-rpc 46 passed plus the existing live-Hacknet infrastructure ignore, and strict queue/RPC Clippy are green.

## 2026-08-16T22:23:09Z

Added the P2P retention slice on top of the shared nano-queue abstraction. A session still has the independent 256-item guard but now retains at most 16,778,054 wire bytes—exactly one maximum protocol frame—instead of up to 256 maximum frames. Both relay directions retain at most 1,024 items and 32 MiB of canonical block/transaction bytes, shed oldest on either limit, recover after drain, and expose items, bytes, oldest age, drops and saturations. The real maximum-frame regression, byte/count shedding, drain recovery, nano-queue 8/8, nano-p2p all targets (28 unit + 8 loopback; configured live probes absent), downstream nano-node all-target check, and strict nano-queue/nano-p2p/nano-node Clippy are green. Per-address/global pre-allocation budgets and the all-session load plateau remain open.

## 2026-08-16T22:38:59Z

Completed the P2P pre-allocation boundary. Every production inbound and outbound session now shares one FrameBudget: after the fixed 165-byte preamble is validated, the complete advertised wire length is reserved by peer IP before any payload read or decode. The default node budget is eight maximum frames globally and two per IP; permits follow decoded messages through buffering and release on processing/shedding. The listener also admits at most four concurrent sessions per IP and drops excess sockets before spawning, while retaining the independent 64-inbound-session cap. Focused tests prove per-IP/global saturation and recovery, eight-maximum-frame plateau, header-only refusal before payload delivery, and address-slot recovery. nano-p2p is green at 30 unit + 9 loopback; nano-node 89/89; 17 stacks-core/P2P conformance cases pass with three explicitly unconfigured mainnet probes; strict P2P/node/conformance Clippy is green. The conformance build also exposed and fixed proposal_failover's stale unbounded test channel from the earlier RPC migration. Metrics publication and sustained RSS/task load evidence remain open.

## 2026-08-16T22:51:55Z

Added explicit admission policy to every public RPC route: request bodies are capped by wire shape (zero for reads; 1 MiB mempool query; 2 MiB transaction; 4 MiB call-read/chunk/block classes; 8 MiB proposal JSON), each route has an independent concurrency/rate budget, and all routes share a 128-request node ceiling. Body overflow returns 413 before extraction; rate overflow returns 429 + Retry-After; concurrency overflow returns 503 + Retry-After; rejected requests never enter the handler. Pinned stacks-core efc34a0 confirms valid mempool sync is at most 2,048 tags or the 8,192-entry Bloom filter, so its 1 MiB cap is compatible. Middleware tests prove concurrency/rate refusal and recovery; the route table proves every body route rejects Content-Length one byte over its declared cap. Final nano-rpc suite: 50 passed, 1 existing live-Hacknet ignore; nano-node all-target check and strict nano-rpc/nano-node Clippy are green with no warnings. Slow headers/chunked bodies and published admission metrics remain the next slice.

## 2026-08-16T23:22:23Z

Bound slow and amplified RPC transport work without claiming the remaining Clarity execution boundary: the production HTTP/1 server now admits at most 256 connections globally and 16 per IP, caps its buffer at 64 KiB, closes incomplete headers after 10 s and all connections after 15 min; route bodies have a 10 s idle timeout and 5-60 s class deadline; fragmented body frames share one byte counter; pre-allocation checks cap Clarity arguments at 2 MiB, chunk/block hex at their decoded protocol limits, and mempool queries at 8,192 tags/128 KiB. A P2P regression proves one-byte fragments cannot restart the absolute message deadline. The real socket-to-mempool-to-relay-to-miner execution gate was repaired to include the required bounded relay. Evidence: nano-rpc 56 passed/0 failed/1 configured live-Hacknet ignore; exact fragmented P2P 1/1; exact submitted_transaction 1/1; strict all-target Clippy across nano-p2p/nano-rpc/nano-node/nano-conformance green. The task checkbox stays open until synchronous read-only Clarity work has its own bounded worker and consensus cost budget.

## 2026-08-16T23:29:33Z

Closed the slow/request-work item with the separate Clarity execution boundary. Public call-read now enters one of four permits before spawning synchronous VM work off Tokio; the permit lives inside the blocking job, so an HTTP timeout or disconnected client cannot make still-running work disappear from accounting, and a fifth call receives explicit 503 + Retry-After until real work completes. The VM loads the pinned stacks-core read-only ceiling (runtime 1,000,000,000; read length 200,000; read count 100; writes zero) instead of a free tracker, while consensus transactions retain the Epoch 4 block limit. The regression aborts all four request futures, proves capacity stays full, then proves recovery. A stale nano-vm test was corrected so an unclaimed development state may adopt another test chain while unprofiled mainnet remains fail-closed. Evidence: focused worker 1/1; captured explicit-limit tracker 1/1; nano-vm 49 passed/0 failed/1 intentional search ignore; nano-rpc 57 passed/0 failed/1 configured live-Hacknet ignore; strict all-target Clippy for nano-vm/nano-rpc/nano-node green.

## 2026-08-16T23:36:11Z

Closed a post-admission StackerDB memory escape before publishing queue metrics. The replication loop had drained its bounded 256-item/16-MiB nano-queue into an ordinary retry Vec, releasing byte permits while failed work remained retained; repeated peer failures could therefore accumulate another full queue each round outside accounting. Pending chunks now remain nano_queue::Lease values through every retry and release capacity only after successful delivery or shutdown. The focused regression proves 6/8 bytes remain charged after drain, the next 3-byte write is refused, and capacity recovers when retained work clears. Evidence: exact hosting regression 1/1; strict nano-node all-target Clippy green; rustfmt and diff-check green.

## 2026-08-16T23:41:59Z

Published the first producer-owned ingress queue metrics without duplicating accounting. The block-upload, proposal, StackerDB, transaction, relay-offered and relay-announcing queues now expose labeled current items/bytes/oldest age and cumulative drops/saturations from the exact nano-queue/relay snapshots that enforce their limits. Relay status now preserves per-direction shedding instead of only an aggregate. Existing count-only sync-status gauges remain compatible. Evidence: affected all-target check green; nano-node 90/90, nano-p2p 31/31, nano-rpc 57/57 plus one configured live ignore; golden Prometheus scrape and relay direction regressions green; strict all-target Clippy across all three crates green. Observer, session/frame, route and connection metrics remain open.

## 2026-08-16T23:50:18Z

Completed the second metrics slice. Every nano-queue status now reports its configured item/byte capacity as well as current load, age and shedding; those limits are exported beside the six existing ingress queue samples. Each event observer gets its own stable event_observer_N metric label and updates metrics on enqueue, saturation and delivery release. Outbound P2P session push buffers are aggregated across live sessions without losing per-session capacities, oldest age, drops or saturation, using the one metrics registry created before transport startup and later served by RPC. Evidence: deterministic observer queue saturation scrape 1/1; session aggregate 1/1; nano-queue 8/8, nano-p2p 32/32, nano-rpc 58/58 plus one configured live ignore, nano-node 90/90; affected all-target check and strict four-crate Clippy green. RPC route/connection and P2P frame admission metrics remain the next slice.

## 2026-08-16T23:58:21Z

Published every public RPC route's admission policy and live use. The metrics registry now exposes per-route body-byte, concurrency and requests-per-second limits, currently active handlers, labeled concurrency/rate refusals, plus node-wide active requests, the 128-request ceiling and global saturation. Permits decrement gauges on every normal, timeout and cancellation drop. Focused middleware tests drive a real held request, explicit 503, recovery and a 429, then scrape the exact metrics. A temporary test hang exposed and fixed a Family lock-guard lifetime bug before checkpointing. Evidence: route metrics tests 2/2; nano-rpc 58/58 plus one configured live ignore; nano-node 90/90; affected all-target check and strict Clippy green; rustfmt/diff-check green. TCP connection and P2P frame/session admission metrics remain open.
