## 2026-08-09T17:25:09Z

2026-08-09: Ran the complete tenure_continuity module with NANO_MAINNET_CAPTURE=/home/aldur/mainnet-capture: 4/4 passed, 0 skipped. The captured control executed; mutations of the previous-tenure identity and block count were rejected; the mainnet sample checked its one observable tenure identity and honestly reported that it contains no complete prior tenure for count comparison.

## 2026-08-12T03:03:34Z

2026-08-12: checkpoint a18040dd authenticates every followed block's miner against the locally derived sortition winner, counts successful per-block and tenure-start checks, and refuses a sealed restart without its committed ledger. Green isolated gates: nano-chainstate 34/34; nano-node 61/61; tenure_vrf_enforcement 9/9 including a captured mid-tenure forged-winner refusal; checkpoint_authentication 5/5; checkpoint-history atomic refusal; checked-in execution-only capture validator; restart 4/4; all-target check for nano-chainstate/nano-node/nano-conformance; configured all-target Clippy with -D warnings. The opt-in global pedantic/no-deps audit remains separately red on 77 pre-existing public-API documentation/value-size lints, not this batch. Task 076 remains open until a fresh state built from the real mainnet checkpoint, authentication suffix, boundary, signer set and accounting completes its attested replay/follow window with zero fail-open lines.

## 2026-08-12T09:44:12Z

2026-08-12 — Fresh-import attempt is NOT acceptance evidence. A genuinely empty state at /home/aldur/task076-fresh.20260812.dNPpoH/state-fresh started with the real checkpoint, authentication-history suffix, boundary proof, attesting reward set and accounting, and logged checkpoint signer attestation 2,708/2,599. After about 1h54, while another workspace-wide release build and a separate replay overlapped on this 31-GiB/no-swap host, the kernel OOM killer killed stacks-node PID 1565867 (about 6.1 GiB anonymous RSS). The partial directory has marf.sqlite but no clarity.sqlite or staging.sqlite and is retained only as failure evidence; it must not be resumed or counted. Task076 remains open and requires a new empty directory with the import as the sole heavyweight workload. Log: /home/aldur/task076-fresh.20260812.dNPpoH/task076-fresh-import-eef88ee2.log.

## 2026-08-12T16:42:38Z

2026-08-12: The current release artifact reopened the real-checkpoint replay configured with the 26-block authentication-history suffix and boundary proof, recovered the parent-tenure proof and all 2,477 leader keys, then published a 1,000-block batch through 8,703,679. The production summary reports all 1,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and all 17 tenure-start coinbase-VRF/parent-seed checks passed. The log contains zero fail-open 'cannot check' lines and zero block refusals. This is current follow-window evidence, but the task remains open until the same fresh lineage completes the captured replay/follow window.

## 2026-08-12T19:19:05Z

2026-08-12: The same fresh authenticated lineage, now on current commit dd88e92d, reopened at 8,707,267 with its bounded ledger, parent-tenure proof and 2,477 checkpoint leader-key registrations, locally derived 144 sortitions, and sealed/published 1,000 blocks through 8,708,267. Production reported all 1,000 block envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and all 17 tenure-start coinbase-VRF/parent-seed checks passed; the log has zero fail-open `cannot check` lines and zero block refusals. This is another restart-tested authenticated window, but Task076 remains open until this lineage completes the captured replay/follow window.

## 2026-08-12T19:54:58Z

2026-08-12: The same fresh authenticated lineage advanced five exact 1,000-block batches from 8,707,267 through 8,712,267. Production reported 5,000/5,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 96/96 tenure-start coinbase-VRF/parent-seed checks passing. The continuation log contains no fail-open 'cannot check' line and no positive block-refusal summary. The service remains live and Task076 remains open until the full captured replay/follow window reaches tip.

## 2026-08-12T20:22:44Z

Prepared the cycle-141 signer gate while the authenticated replay runs: NANO_MAINNET_SIGNER_EVIDENCE now names a separate retained stacker-set/new_block bundle, so the old cycle-140 checkpoint capture no longer forces a false skip. SignerWeights factors the same ordered threshold verification over the event-recorded block digest. rustfmt, git diff --check, and taskmd validation pass; Cargo and the live gate remain pending until the replay releases the serialized memory budget and crosses burn 962150.

## 2026-08-12T20:43:49Z

Added and dry-ran scripts/capture-mainnet-signer-evidence.sh. It derives the requested cycle from the retained /v2/pox calendar, requires nano and every supplied stock node to publish byte-equivalent stacker-set JSON, selects only observer blocks whose burn heights fall in that cycle, and writes a hashed manifest atomically. A six-block cycle-140 control captured heights 8695779..8695784 and all hashes verified. The conformance consumer likewise derives cycle from burn height because new_block.cycle_number is legitimately null. ShellCheck, rustfmt, diff-check, and taskmd validation pass; cycle141 capture awaits the active replay crossing burn 962150.

## 2026-08-12T20:56:34Z

The same fresh authenticated lineage has now executed 10,000 consecutive current-artifact blocks, 8,707,267→8,717,267. Production reported 10,000/10,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 186/186 tenure-start coinbase-VRF/parent-seed checks passing. The stable log prefix through the tenth summary contains zero `cannot check` lines, zero authentication failures, zero state-root mismatches and no positive peer-pushed block refusal. The service remains the same PID with zero restarts and zero cgroup OOM events. Task076 remains open until this lineage reaches tip and executes into cycle 141, where its recorded signer set and accepted block signatures can be compared with the retained stock-node evidence.

## 2026-08-12T21:26:05Z

The same fresh authenticated lineage has now executed 15,000 consecutive current-artifact blocks, 8,707,267→8,722,267. Production reported 15,000/15,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 274/274 tenure-start coinbase-VRF/parent-seed checks passing. The stable log prefix through the fifteenth summary contains zero `cannot check` lines, zero authentication failures, zero state-root mismatches and no positive peer-pushed block refusal. The service remains PID 782306 with zero restarts and zero cgroup max/OOM/OOM-kill events. Task076 remains open until this lineage reaches tip and executes into cycle 141, where its recorded signer set and accepted block signatures can be compared with retained stock-node evidence.

## 2026-08-12T21:50:00Z

The same fresh authenticated lineage has now executed 20,000 consecutive current-artifact blocks, 8,707,267→8,727,267. Production reported 20,000/20,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 361/361 tenure-start coinbase-VRF/parent-seed checks passing. The stable log prefix through the twentieth summary contains zero `cannot check` lines, zero authentication failures, zero state-root mismatches and no positive peer-pushed block refusal. The service remains PID 782306 with zero restarts and zero cgroup max/OOM/OOM-kill events. Task076 remains open until this lineage reaches tip and executes into cycle 141, where its recorded signer set and accepted block signatures can be compared with retained stock-node evidence.

## 2026-08-12T22:16:29Z

The same fresh authenticated lineage has now executed 25,000 consecutive current-artifact blocks, 8,707,267→8,732,267. Production reported 25,000/25,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 444/444 tenure-start coinbase-VRF/parent-seed checks passing. The stable log prefix through the twenty-fifth summary contains zero `cannot check` lines, zero authentication failures, zero state-root mismatches and no positive peer-pushed block refusal. The service remains PID 782306 with zero restarts and zero cgroup max/OOM/OOM-kill events. Task076 remains open until this lineage reaches tip and executes into cycle 141, where its recorded signer set and accepted block signatures can be compared with retained stock-node evidence.

## 2026-08-12T22:35:00Z

Prepared the cycle-141 evidence gate without changing the release binary under replay. The capture now retains each accepted event's raw `/v3/blocks/:index_block_hash` bytes, and conformance decodes that envelope, requires its digest and signatures to equal the event, then applies the existing production `SignerWeights::verify(&header)`. The temporary public digest-only verifier and its production unit assertion were removed, restoring `nano-chainstate/src/nakamoto.rs` to the dd88e92d artifact's source instead of asking a later production binary to inherit replay evidence. Bash syntax, rustfmt, diff-check and taskmd validation pass; the live criterion remains open until cycle-141 evidence is captured and the Rust gates run.

## 2026-08-12T22:43:14Z

The same fresh authenticated lineage has now executed 30,000 consecutive current-artifact blocks, 8,707,267→8,737,267. Production reported 30,000/30,000 envelope/miner-signature/winner-sortition/signer-threshold/tenure-continuity checks and 526/526 tenure-start coinbase-VRF/parent-seed checks passing. The stable log prefix through the thirtieth summary contains zero `cannot check` lines, zero authentication failures, zero state-root mismatches and no positive peer-pushed block refusal. The replay's local burn view has entered cycle 141; reward-set publication and retained block evidence are still pending, so this does not prematurely close the live signer gate. The service remains PID 782306 with zero restarts and zero cgroup max/OOM/OOM-kill events.
