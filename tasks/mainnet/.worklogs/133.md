## 2026-08-15T19:38:50Z

2026-08-15: Initial current-tree audit. release-report still builds target/release/stacks-node from the mutable checkout, reports dirty state without failing, and has no candidate manifest, SBOM, advisory verdict, signature, independent rebuild comparison, freeze identity, or packaged service profiles. The pinned flake supplies cargo-cyclonedx, cargo-deny, minisign and Nix rustPlatform builders, so the implementation will use those vetted tools: clean Nix artifact closure; pinned RustSec policy; signed candidate manifest; report verification; deterministic final bundle including the qualification report; and explicit systemd/container profiles. Task moved to in-progress before source edits.

## 2026-08-15T20:38:38Z

Clean flake build for 10f92914 compiled successfully in 11m30s, then failed only in postInstall because cargo-cyclonedx writes the nano-node SBOM to crates/nano-node/nano-stacks-sbom.json. An isolated git-archive probe confirmed root component nano-node, 430 components, and deterministic SOURCE_DATE_EPOCH timestamp; corrected the install source path.

## 2026-08-15T20:52:57Z

Clean committed flake build 43005dde succeeded: /nix/store/2znnpy2rmw38rmq9l5w3wsg9arrcf3jm-nano-stacks-0.1.0-43005dde0085. Packaged binary SHA-256 6304a704276e2ee669006928157c93636cf1dbe7746262708b99a4522391e25d; CycloneDX 1.5 SBOM root nano-node with 430 components; deterministic epoch timestamp; schema/dependency tree/systemd/container/build identity present; NAR sha256-shAbfbrfeIIuq0NY3fGSbR7CgrFgAHlaFeFhhVAILK0= and 93,438,136-byte closure. Inspection found build-identity reports Wasmtime 15.0.0 while Cargo.lock/SBOM resolve 15.0.1.

## 2026-08-15T20:52:57Z

Pinned RustSec DB 69f93e1d081d8b6fbee010e48f0b5e0d13661415 is clean, but cargo audit --no-fetch --deny warnings is RED: 17 Wasmtime 15.0.1 vulnerabilities plus ten unsound/unmaintained warnings. No exception was granted. Task 130 is the existing supported/patched/minimal-engine work; task 133 must keep advisory qualification red until that closure is addressed.

## 2026-08-15T21:09:03Z

Implemented the signed candidate/finalization boundary in xtask: clean Nix package copy, normalized closure, CycloneDX and build-identity consistency, pinned cargo-audit fail-closed policy, config digest without secret contents, checkpoint/source-input hashes, SLSA v1 in-toto provenance, complete SHA256SUMS inventories and minisign signatures. release-report requires a signed candidate for qualification and re-verifies candidate/config/checkpoint after all gates. Six focused tests prove valid preliminary/final signatures and reject mutation, extra files, missing SBOM, unsigned manifests, advisory failures and report rebinding; strict xtask all-target Clippy is green.

## 2026-08-15T21:12:43Z

Added reproducibility/CI enforcement: scripts/reproducible-release.sh builds the exact Git revision in two separate rootless local Nix stores, compares output path, NAR hash and every packaged file SHA-256, and deletes both stores through a validated scratch prefix. Ordinary CI now explicitly runs dirty/stale/candidate rejection tests. Manual qualification requires external config, pinned clean RustSec DB and minisign key paths; it runs the independent builds, prepares one candidate, exercises that candidate, finalizes the report signature and verifies the published directory. shellcheck, actionlint and diff-check are green.

## 2026-08-15T21:44:02Z

2026-08-15: Two independent rootless local Nix stores rebuilt exact clean revision 36958a2d698976db2e853139306bf5b89e863d7e. Both produced /nix/store/icqca9aqbldgi0zm3garv8q3xpb0vfc4-nano-stacks-0.1.0-36958a2d6989, NAR sha256-2xX5gPojKQp0sMbnS0PEpsHQ+vT19zZw1MBmx+DG54I= and stacks-node SHA-256 d9b78816feab0fbd0a2c32ce57a2ad8559a41aecc1dfb908fa65cd9f37402e73; every packaged file matched byte-for-byte. The original run reached PASS, then exposed a cleanup-only bug on read-only Nix store directories. Commit b83abd0f makes the validated scratch directories owner-writable before deletion and preserves a cleanup failure as nonzero. The exact 7.1 GiB leftover was removed with that sequence; shellcheck and diff-check pass.

## 2026-08-16T17:33:01Z

2026-08-16: Post-Task-130 audit found release-candidate advisory verification had become fail-closed for the wrong reason: both exact, unexpired exceptions are owned by now-completed task 130, while the verifier accepted only in-progress/blocked owners. Reproduced with the current pinned advisory DB: release-candidate audit exited 1 before cargo-audit. Narrow lifecycle fix accepts an existing completed owner while still rejecting missing, pending or cancelled ownership; expiry, exact package/version/reachability and zero-vulnerability checks remain unchanged. Focused audit/test/Clippy pending before commit.

## 2026-08-16T17:33:53Z

2026-08-16: Advisory-owner lifecycle fix verified. Focused release_advisory suite: 3/3 passed. Real pinned /home/aldur/.cargo/advisory-db audit now PASS: zero vulnerabilities and exactly two owned, unexpired non-release/test-only exceptions. Strict xtask all-target Clippy passed. This restores release preparation after Task 130 closure without weakening package/version/expiry/reachability or vulnerability checks.

## 2026-08-16T17:35:31Z

2026-08-16: Freeze-rule audit found qualification revalidated the signed candidate/config/checkpoint after gates but did not re-read the workspace. An uncommitted source/lock/toolchain mutation during a long run left HEAD unchanged and could evade the final check. Added a final source freeze verdict requiring both the original revision and a clean build-relevant tree, plus a regression for dirty mutation and a later clean commit. Focused gates pending before atomic commit.

## 2026-08-16T17:36:36Z

2026-08-16: Final source-freeze unit gate passed 3/3, including the new dirty-change/new-commit regression; strict xtask all-target Clippy passed. The release-report CLI suite intentionally short-circuited while these build-relevant edits were uncommitted, proving the initial cleanliness boundary; it will be rerun from the clean atomic commit to exercise later evidence stages.

## 2026-08-16T18:05:12Z

2026-08-16: Current revision 404408b25dd9 built in two independent rootless stores and matched byte-for-byte: /nix/store/3k3m7ap39ww0p9csc7q9zskpps7c7wa1-nano-stacks-0.1.0-404408b25dd9, NAR sha256-+sxxXtH/P3Mdfqd5NzU661MuKlGhGRQ522zTfcgiSeA=, binary SHA-256 73f491124465f626c7bd41c4c03d56de094cfbbac5453f1fa8026a84b713aa75. Validated scratch cleanup passed. Audit then found the compared stores were deleted before candidate preparation, which rebuilt a third artifact. The release script/workflow now imports one compared NAR into the active store, rechecks its NAR and full file inventory, and passes that exact store path to candidate preparation. Static gates and a fresh end-to-end reproduction pending before commit/evidence.

## 2026-08-16T18:32:55Z

2026-08-16: Exact-artifact handoff attempt at 8f03eb29 failed closed after both clean builds: the unprivileged CI user cannot import an unsigned locally built path into the daemon store (Nix correctly required a trusted signature). Scratch cleanup passed and no qualification claim was emitted. Reworked the handoff without weakening daemon trust: retain one already-compared rootless store at an explicit same-filesystem NANO_REPRODUCIBLE_STORE, record its virtual output path, recheck NAR and full file inventory there, and teach candidate preparation to resolve/query that exact store explicitly. Workflow cleanup makes only its owned temp directories writable before removal. Static/compile/end-to-end gates pending.

## 2026-08-16T18:34:12Z

2026-08-16: Rootless-store handoff implementation compiles. Exact artifact resolver/path-traversal regression passed; strict xtask all-target Clippy, cargo fmt, shellcheck, actionlint, diff-check and taskmd validation are green. The next proof is a clean two-store rebuild that retains and rechecks the compared store, followed by real candidate prepare/verify from that store.
