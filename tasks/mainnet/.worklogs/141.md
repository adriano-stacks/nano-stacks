## 2026-08-19T12:30:00Z

Wrote the sealing design against the measured protocol rather than the
imagined one: docs/decision-record-sealing.md records what a committed block
is spread across today (the synchronous=FULL clarity.sqlite transaction
carrying metadata/header/ledger, then the MARF commit as the decision point,
with staging/archive/sortition files beside), evaluates the single-database
fold first as the task requires, and rejects it for coupling a trie storage
format migration and a checkpoint-format change to the same release as the
authority-boundary change (task 140). The chosen design appends the
epoch4-consensus DecisionRecord into the same existing transaction and makes
"record row present AND sealed trie present" the only committed-block
visibility rule — no new fsync, no checkpoint change, crash residue becomes a
defined discarded state, and the existing kill/fault evidence carries over
with three new injection points. Filesystem/WAL/power-loss assumptions and
the four-step migration with its no-fallback ending are stated in the
document. The spec and evaluation tasks are checked; implementation follows
the 140 extraction it builds on.

## 2026-08-20T04:22:59Z

2026-08-20: strengthened the fault-injection item's evidence rather than its scenario count, because the scenarios already existed and the assertion did not. storage_faults.rs injects at every durable commit boundary through an indexed libfiu sweep, plus EIO and ENOSPC on read and write paths, a read-only filesystem, a corrupted first page and mismatched store generations - but the Fingerprint each run was compared against carried tip, state root, accounting, executed set, parent tenure proof and tenure start, and not the decision record. A recovery that reopened a tip with its root and accounting intact but no record would have passed every one of those scenarios, which is exactly the partial visibility this task's first acceptance criterion forbids. 17810af5 adds the record's content hash to the fingerprint, so all three tests now assert the criterion across every boundary they already covered, and adds reference_fingerprint, which requires the clean replay's tip to carry a record: without that, a record that never landed on either side would compare equal as None and the criterion would only look satisfied. Evidence: scripts/storage-faults.sh green, 1 + 3 tests passed in about 10 seconds, strict all-target nano-conformance Clippy and repository formatting clean. The runs also show the orphan-record path from 7a81afd8 firing inside these scenarios - 'discarding the decision record for <id>: the trie never sealed it, so it was never committed' - so the sweep genuinely produces records the trie never sealed and recovery discards them. Power-loss simulation and interrupted-migration injection remain open on this item, and the migration itself does not exist yet.
