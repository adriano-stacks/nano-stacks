## 2026-08-12T19:34:21Z

2026-08-12: Added scripts/watch-mainnet-tip.sh so the 24-hour clock can start immediately after Tasks037/054 qualify the same binary. It refuses a nonempty output, a config naming hosted Hiro APIs, a missing/replaced/restarted process, or an initial node with nonzero blocks_behind, no P2P session, or an unreachable/backlogged observer. Once started it records immutable binary/config hashes and one JSON sample per minute for exactly 86,400 seconds: /v2/info, full /nano/sync_status (tips, peer set, queues and observer backlog), selected refusal/failover/queue metrics, service memory current/peak, open FDs, free disk and each durable DB size. Any RPC/process failure appends a terminal failure record and exits nonzero, forcing a fresh full interval. bash -n, Nix ShellCheck and diff-check are green. A read-only smoke against the still-catching-up dd88e92d node exited 1 with `the release node is not caught up and observable`, proving the timer cannot start early. The tiny smoke output is retained at /tmp/nano-soak-smoke.sxrjWO. Task106 remains pending until the same-binary clean replay/no-hosted qualification reaches tip.

## 2026-08-12T19:41:19Z

2026-08-12: Corrected the sampler before qualification: `/v2/info.burn_block_height` is the burn view attached to executed Stacks state, not necessarily the external Bitcoin network tip. The script now requires an explicit read-only Bitcoin-tip URL, validates its response as a decimal height before starting, records the URL and initial height in provenance, and samples the height every minute. `bash -n`, Nix ShellCheck and diff-check remain green; a second read-only smoke against the catching-up node queried mempool.space successfully and still failed closed before starting the timer. Output retained at /tmp/nano-soak-bitcoin-smoke.g7RLsk.

## 2026-08-12T20:35:14Z

Prepared the mandatory per-block oracle half of the 24-hour hold. verify-mainnet-observer compares block identity, consensus-header state root, transaction order/status/result, all five costs, and every ordered event (including paginated receipts) against the independent Hiro stacks-node/API record. The watcher now requires NANO_TRACE_ROOTS, drains every new observer block through the verifier, retries unavailable oracle reads without making them node liveness inputs, and refuses completion with a pending or unequal block. A five-transaction block passed exactly; a 1,071-event transaction proved pagination and normalization exact. Bash syntax, Nix ShellCheck, Ruff, and diff-check pass. The live 24-hour gate remains pending until task037 reaches tip.

## 2026-08-19T09:55:00Z

The hold now has a watcher shaped for the artifact that will actually be held.
The program moved the release candidate from the development node to the
follower-only artifact under task 135, and the follower deliberately serves
only loopback /health and /metrics — so scripts/watch-mainnet-tip.sh, which
reads /nano/sync_status, an event observer and NANO_TRACE_ROOTS lines, cannot
watch it. scripts/hold-follower-mainnet.sh (committed today) samples once a
minute what the follower exposes — health, metrics, process RSS/peak/FDs/start
ticks, executable hash, database and WAL sizes, free disk, both stock oracles'
tips and an external Bitcoin height — and verifies every block the follower
executes three ways: byte equality against both independent stock oracles
(byte equality subsumes the header state root the follower already refused to
seal without), archived receipt commitment against the digest of a witness
node's new_block payload (crates/nano-conformance/src/bin/receipt-digest.rs),
and the witness payload field by field against the receipt oracle through the
existing scripts/verify-mainnet-observer.py. Oracle unavailability is a retry
and never a liveness input; any process change, mismatch or missing evidence
is a terminal failure record that restarts the 24-hour clock from zero.

The witness is deployed: the development node at /home/aldur/mainnet-tip now
posts to an event sink (docker nano-hold-sink, loopback-published 20470,
writing /home/aldur/mainnet-hold-receipts/new_block/), and one fresh payload
already passed verify-mainnet-observer.py against the receipt oracle with the
state root read from a stock oracle's raw block. The hold itself starts only
after the packaged follower finishes its checkpoint-to-tip catch-up and the
restart qualification behind wait-and-qualify.sh, so the measured interval
contains no planned recovery event. scripts/analyze-follower-hold.py reduces
the finished run to the acceptance questions, including least-squares
resource trends.

## 2026-08-19T19:55:00Z

The 24-hour clock is running: hold-16e0928a-20260819T152727Z.jsonl under
/home/aldur/mainnet-hold, started 15:27:27Z against the qualified packaged
follower (exe SHA-256 2e26681c…, pid discovered and pinned with its start
ticks, config peers = [] verified), sampling once a minute and verifying
every executed block against both independent stock oracles. At 19:42Z:
1,063 blocks verified, 207 samples, zero failures.

Two failed attempts preceded it, each stopped by the watcher's own
fail-closed design within a minute, and each taught something now committed:

1. 15:14Z, receipts: the inline witness was the development node on a stale
   revision whose compiler lacks the map write-cost fix — its payload for
   block 8,802,356 digested one write_length unit apart from the follower's
   commitment, and Hiro's independent record sided with the follower (423
   against the stale node's 422). The receipts comparison is now deferred to
   scripts/verify-hold-receipts.sh against a same-revision witness; a fresh
   16e0928a witness node with an event observer is importing the signed
   bundle and will re-execute the entire hold window, so every hold block's
   receipt commitment gets verified retroactively, plus field-by-field
   against the receipt oracle.

2. 15:15Z, bytes: the follower archived a fourteen-signature representation
   of block 8,802,451 while oracle A served a fifteen-signature one — the
   signer signature vector is outside the block hash, so byte equality was
   the wrong claim. The watcher now compares consensus identity through the
   block-identity binary (block id, block hash covering every header field
   and the transaction merkle root, sealed root), which is exactly what two
   valid representations must agree on.

The evidence run that is now under way therefore asserts, per block:
identity equality against two independent oracles, the archived receipt
commitment recorded for the deferred pass, and the follower's own health,
process and resource samples. Completion lands 2026-08-20 15:27Z; the hold
is release evidence only together with the receipt verification pass.

## 2026-08-20T01:15:00Z

The first full interval died at 00:25:02Z, nine hours and 2,229 verified
blocks in, on a single missed ten-second health probe — the follower answered
again seconds later and had no error, while the host was concurrently
importing the 22 GB checkpoint twice for the epoch4 shadow gate and catching
the receipt witness up. The watcher was right to stop (an unobservable
follower is indistinguishable from a failed one at that instant) and the
interval is preserved as hold-…health-probe-artifact.jsonl, but one missed
probe under an I/O storm is a measurement artifact, not a node defect: the
probe now retries three times across half a minute before declaring failure,
and a follower that stays unresponsive still fails the hold. A fresh 24-hour
interval started at 01:11:26Z and completes 2026-08-21 01:11Z.

## 2026-08-20T06:30:00Z

The second interval died at 05:52:09Z after 1,179 clean blocks, and this
time the root cause was operator-inflicted disk exhaustion: the epoch4
shadow gate's two durable checkpoint imports (68 GB) filled the volume to
3 GB free, Bitcoin Core protected itself with a clean self-shutdown at
05:51:04Z, the follower — whose burnchain preflight and round loop need that
RPC — stalled with its HTTP surface wedged (the same silent-stall class the
StallWatch fix now reports in the tree; the frozen artifact predates it),
and the hold's three health probes correctly failed. The interval is kept as
hold-…bitcoind-enospc.jsonl. Recovery: freed the shadow workdir (the gate is
green and its logs are preserved), recreated the transient bitcoind unit,
restarted the follower service — it resumed from durable state and re-reached
tip within minutes with no divergence — and started interval four at
06:25:41Z, completing 2026-08-21 06:25Z. Nothing disk-heavy runs on this
host until the hold completes.

## 2026-08-21T16:20:00Z

The sixth interval completed clean: 86,431 seconds, 1,165 samples, 6,243
blocks (8,808,750 through 8,814,993) each verified by consensus identity
against two independent stock oracles, zero failures, zero retractions,
worst executed-tip lag six blocks, two isolated unobserved samples, resource
slopes consistent with cache fill and chain growth (hold evidence SHA-256
f9c3cabf…aa65fb, analyzer output in the worklog history). The interval
itself satisfies the continuous-24-hour half of this task.

The deferred receipts pass then did exactly what it exists to do: on its
second block it found nano charging more than the canonical record —
read_length +969, runtime +4,048, write_length +560 on a successful
trait-dispatch marketplace call, with the state root byte-identical and both
nano lineages agreeing with each other against the oracle. That is a real,
long-standing cost differential, now owned as task 146 with the full
measurement; a block-level cost sweep across the whole window is running to
scope it. Under the plan's own rule a cost differential blocks release
whether or not a block hits the limit, so this task stays open until 146
closes and a hold window's receipt verification passes end to end.

## 2026-08-22T12:35:40Z

Task 146's fixes change what this task's re-run requires, and the change is structural rather than a delay. The three charging fixes altered the compiler, so the compiler identity and profile fingerprint moved (sha256:1f78d344 -> a committed-tree identity, 5561d364 -> 1d7b821a at the time of writing). A node refuses to start against a checkpoint bundle whose recorded compiler identity is not its own, and the [checkpoint] section is mandatory even when the state is already imported, so the attested bundle this hold ran from is not merely stale — it cannot be imported by a fixed node at all.

That means the hold interval already gathered (hold-16e0928a-20260820T155354Z.jsonl, 86,431 seconds, 6,243 blocks, zero failures) documents an artifact that will not be the release candidate, and the deferred receipt verification cannot simply be re-run against a rebuilt binary. The sequence is: finish 146's runtime dimension, re-run the checkpoint builder ceremony for the new compiler identity so an attested bundle exists, import it fresh, and hold again. The 24-hour apparatus itself needs no further work — scripts/hold-follower-mainnet.sh completed a clean interval on its sixth attempt and its failure modes are all now handled — so what this task owes is a re-run on a qualifying artifact, not new tooling.

## 2026-08-26T12:38:52Z

2026-08-26: rebuilt the hold's subject on the final compiler. release-subject-88920833 could not be carried forward -- it was dead on block 8,832,029's cost differential (task 149) and its compiler identity is superseded twice over -- so the 8,665,600 checkpoint was re-attested for compiler sha256:32862f69 / profile 7a374d6d (content root 3deca3ada868, two independent manifest builds agreeing, both builder keys, offline verify green, every earlier attestation byte-identical) and release-subject-bbe654e3 is importing from it with peers = [] . Health on 127.0.0.1:20478, metrics on :20479. disk-guard.sh armed at a 40 GiB floor against 499 GiB free, and progress-sampler.sh writes progress.jsonl once a minute so the import and the interval that follows are auditable without ps. Measured import rate on this filesystem is ~4.5 GiB/hour against ~55 GiB of MARF, so the import alone is ~10 hours; the source bundle is four generations of reflink deep, which is the likely cause. Two stale followers were stopped to free the I/O: the port-20492 diagnostic node (hand-repinned profile, superseded) and follower-import-final-16e0928a, which had been looping on the already-fixed task-147 defect at 8,815,026 for ~11 hours of CPU.

## 2026-08-26T12:48:53Z

2026-08-26: the whole remaining chain is now unattended. Topology matches what this task requires: subject = the packaged follower artifact release-subject-bbe654e3 (health 20478, metrics 20479, peers = []), witness = witness-bbe654e3, a same-revision stacks-node with the event role writing new_block into a clean sink (nano-hold-sink-bbe654e3 on 20482 -> /home/aldur/hold-receipts-bbe654e3), because the artifact omits the event role by policy and a stale-revision witness already failed a hold once. Both are revision bbe654e37f75, compiler sha256:32862f69, profile 7a374d6d, imported from the bundle whose content root 3deca3ada868 both builder keys signed today. scripts/run-hold-bbe654e3.sh waits for both to import and reach the tip judged against two stock oracles, runs the committed hold-follower-mainnet.sh for the interval with HOLD_BITCOIN_TIP_URL on mempool.space, then runs verify-hold-receipts.sh against the witness payloads and the receipt oracle. block-identity and receipt-digest are built. No stall supervisor runs, deliberately: the harness treats a pid change during the interval as fatal to it, and disk-guard.sh is the only thing permitted to signal. Measured import rate is ~4.5 GiB/hour against ~55 GiB of MARF each, so the two imports plus catch-up are ~19 hours before the interval starts.
