## 2026-08-16T19:38:06Z

2026-08-16: Started literal acceptance audit on clean main at 5a8d8325. No compatibility profile or prior worklog exists. Scope is not a prose summary: one canonical machine-readable profile plus implementation-neutral vectors, independent stock-reference execution, fingerprint admission at checkpoint/state/release boundaries, mandatory vector ownership and fail-closed unknown activation. Live follower and Cargo queue remain untouched during the source audit.

## 2026-08-16T19:47:59Z

Added the first executable-profile slice: new nano-consensus-profile crate, canonical mainnet Epoch40 JSON, exact PoX/reward/limit/system-source/reference evidence, eight-surface implementation-neutral vector corpus, and an active fingerprint that incorporates compiler plus Wasmtime version/config. Unit tests 3/3 and strict crate Clippy are green. This is deliberately not closure: checkpoint/state/release admission and two stock-reference runners remain.

## 2026-08-16T20:07:10Z

Bound the executable profile to mainnet state admission, checkpoint import/adoption, node build identity, Nix release artifacts, prepared-candidate verification, qualification input verification, and release-report evidence. Missing or mismatched fingerprints now refuse rather than mutate state; compiler/host changes require fresh checkpoint import and full replay. Focused checkpoint/state/candidate tests and strict all-target Clippy are green. Release-report integration remains to rerun from the clean commit because its intended dirty-tree guard stops before test-specific assertions.

## 2026-08-16T20:23:51Z

Corrected the source-evidence model after comparing both named stock revisions and the captured mainnet side store. Deployed pox-5 is ffad35ad (matching 6d58b498), while efc34a07 carries older e666099c; deployed sip-031 is fd393bd3 after its mainnet recipient substitution, while both revisions carry template 7e92b6a4. The profile now separates deployed, generated, template and native sources, records per-revision hashes, and makes both differences explicit instead of silently choosing one source.

## 2026-08-16T20:37:09Z

Added the nano-side executable vector runner. One mandatory conformance test dispatches all eight checked-in IDs through production limits, network identity, payout-window, signer-weight, semantic-epoch, receipt-oracle, cost-limit and activation-refusal APIs; unknown IDs fail. The stateful receipt vector is explicitly bound to the task086 mainnet replay and ignored-test qualification policy. Block authentication now uses the profile crate's version admission predicate; both the all-vector runner and the existing wrong-epoch header refusal are green, as is strict all-target Clippy.

## 2026-08-16T20:52:52Z

Added implementation-independent stock runners pinned to efc34a07 and 6d58b498. Each standalone crate parses the checked-in profile/vectors directly, rejects unknown or duplicate vector IDs, and exercised all eight surfaces through stock constants/APIs. Both cargo check --all-targets, executable JSON reports (8/8 PASS), and strict -D warnings Clippy gates are green under the serialized build lock. The receipt surface is deliberately scoped: external oracle envelope plus stock Clarity consensus-codec round trip; the named task086 gate remains the stateful root-verified execution owner.

## 2026-08-16T21:11:33Z

Release qualification now owns the vector matrix explicitly. New cargo xtask compatibility-vectors requires the exact nano test to report 1 passed/0 ignored, runs both stock manifests with --locked, parses their JSON, and rejects missing/duplicate/failed/stale vector evidence. The end-to-end command is green: nano 1/1 and both stock revisions 8/8. --no-gates names every unexecuted runner/vector and cannot qualify. Focused parser test 1/1, all-target xtask check, strict Clippy, rustfmt and diff-check are green.
