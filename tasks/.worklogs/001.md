## 2026-07-27T16:13:54Z

Verified fmt, clippy with warnings denied, workspace tests, and scoreboard. A live pox-5 node is reachable at its v2 API and reports stacks-core 4.0.1, but its public gateway rejects the required v3 tenure endpoints and no local hacknet/bitcoind/PCS export is present.

## 2026-07-27T16:17:23Z

Added fixture-mode parsing and offline structural validation. Baseline fixtures now fail closed: `cargo xtask validate-fixtures` exits nonzero until all required captured sources exist, while `scoreboard` remains the required deterministic block-1 baseline.

## 2026-07-27T16:18:32Z

Inspected stacks-network/hacknet master (bf821e9d556eab8c7a30c6e86a7dc1f9b200f1a1) and its supplied chainstate archive. It contains epoch-4-era Nakamoto data (heights 262–288) and sortition snapshots (burn heights 0–264), but no event-observer payloads or PCS checkpoint export. Added capture-mode validation that requires all oracle inputs and provenance.

## 2026-07-27T16:18:46Z

Blocked: the host has no Docker/Podman runtime, the public pox-5 gateway rejects /v3 endpoints, and the upstream hacknet archive lacks both receipt events and a PCS checkpoint export. These are required oracle inputs under plan.md before M1 may start.

## 2026-07-27T16:24:31Z

Resumed after updated project guidance explicitly authorizes Nix-provided tooling. Investigating a rootless container runtime to boot the upstream hacknet and capture M0 fixtures locally.

## 2026-07-27T16:43:40Z

Booted fresh Hacknet Epoch 4 with Docker and captured ten canonical post-activation Nakamoto blocks, matching Bitcoin blocks, sortition snapshots, event receipts, reward set, and stable raw MARF source. Added cargo xtask capture-fixtures; it requires an explicit PCS checkpoint and keeps fixtures baseline until one is supplied. fmt, clippy -D warnings, and workspace tests pass.

## 2026-07-27T16:48:22Z

Completed real captured fixture set: ten canonical post-Epoch-4 blocks with receipts, seven distinct corresponding burn blocks, complete sortition snapshots, reward-set JSON, and a stable portable MARF checkpoint at Stacks height 109. Checkpoint manifest pins canonical state ID and header state root. validate-fixtures, scoreboard, fmt, clippy -D warnings, and workspace tests are green.
