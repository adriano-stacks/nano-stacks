## 2026-07-29T10:51:29Z

Started. Core-main isolated Hacknet stalls because pox5-setup exits successfully before deploying its prerequisite contracts; replacement assertions must require deployment and post-swap block growth.

## 2026-07-29T12:46:00Z

Revised finding: pox5-setup did deploy `sbtc-token` and `sbtc-registry`. Core main `efc34a0` reaches the PoX-5 reward-set boundary, but API `9.0.1` rejects Core's `stake-update` print because its codec expects `prev-unlock-height` while Core emits `prev-unlock-cycle`. The resulting `/new_block` HTTP 500 blocks Core's event dispatcher and stops progress at Bitcoin height 280. A pinned API 9.0.1 compatibility image keeps the raw contract log when that exact stale decoder error occurs; the previously stalled isolated network resumed from height 280 to 297 and accepted the replayed event with HTTP 200.
