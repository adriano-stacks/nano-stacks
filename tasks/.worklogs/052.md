## 2026-08-09T17:24:00Z

2026-08-09: Reproduced the release-audit failure. It was an incomplete /v2/pox fixture, not a staged-data leak: the route correctly refused missing PoX configuration/local Clarity answers after the block and tenure assertions had passed. Installed the real test PoX configuration and scripted chain answers. `cargo test -p nano-rpc no_route_serves_a_block_the_node_has_not_executed -- --nocapture` passes 1/1; strict all-target nano-rpc Clippy passes.

## 2026-08-10T14:57:24Z

2026-08-10: audited the retained mainnet observer corpus against Hiro’s independent historical transaction endpoint. The API now exposes transaction status, result, all five cost dimensions and ordered events, though not a complete historical new_block payload. At block 8,702,046, four of six receipts in /home/aldur/mainnet-receipts match exactly. tx bf21bd13… differs in all five dimensions (nano 3876/18552589/35655321/792/767580; Hiro 3964/18552677/38443953/880/767668) and tx acd19539… differs in runtime (49,169,633 vs 48,938,141). This is an older nano compiler corpus and is honestly red, not an oracle. The current pristine replay must regenerate these blocks and compare the current receipt/event stream to Hiro; matured-reward provenance and reward-set fields still require the stock observer payload. No checkbox changed.

## 2026-08-11T19:05:39Z

2026-08-11: compared 1,971 overlapping retained Hacknet new_block events from stock stacks-core and nano. 1,970 matched on transaction/event counts plus matured-reward and reward-set presence. The sole mismatch was height 2,267: the canonical archived Nakamoto block contains exactly two transactions and nano emitted both matching receipts, while stacks-core appended an observer-only phantom transaction for three scheduled STX unlocks. Implemented the pinned stacks-core behavior without adding it to consensus receipts or root settlement: scheduled unlocks retain their STXMintEvents, attach to a first-position coinbase when present, otherwise emit the zero-cost boot-address token transfer memo 'Block 2267 token unlocks'. Exact retained raw bytes, txid 2a4ced27..., result 0x0703, recipients, amounts and JSON event shape are regression-pinned. Green gates: nano-chainstate 34/34; nano-rpc 37/37 runnable; nano-codec 3/3; scoped all-target check; strict Clippy for nano-codec/nano-vm/nano-chainstate/nano-rpc. Commit e8e3ee39. The checkbox remains partial until a fresh live observer run and the independent mainnet comparison prove the new emitted stream.

## 2026-08-12T10:35:41Z

2026-08-12: Removed the circular miner_txid oracle from the 340-block new_block payload differential. The payload now obtains each miner commitment from the separately captured sortition snapshot at the event's burn height; a static all-event join confirmed every event winner matches, and the exact event_observer::new_block_payloads_match_the_ones_stacks_core_published gate passed 1/1. Strict nano-conformance conformance-test Clippy passed with -D warnings. Both gates ran in a private target/cgroup (4 GiB hard cap; 3.5 GiB test peak, 2.2 GiB Clippy peak). Task remains open for an independent long-window comparison of the three matured-reward provenance fields, which the 340-block checkpoint fixture predates.

## 2026-08-12T12:19:28Z

2026-08-12: The current capped ff068e94 mainnet replay emitted real new_block payloads with non-empty matured rewards. A bounded independent check over the first 18 maturing events (heights 8,695,798..8,697,033) queried Hiro's /extended/v3/blocks/{source-hash}/transactions endpoint, which is populated from stacks-core observer events. All 18 from_stacks_block_hash values, all 18 from_index_consensus_hash values and all 18 coinbase miner addresses matched; all 17 consecutive fee payouts named the preceding independently verified tenure miner. This proves the three formerly omitted provenance fields against an independent mainnet observer-derived source. Task remains partial until the same current replay supplies a green receipt/result/all-five-cost/ordered-event comparison and a non-empty reward-set payload.

## 2026-08-12T12:25:21Z

2026-08-12: Current-replay differential at mainnet block 8,695,878 compared all 11 emitted transaction receipts with Hiro's independent observer index. All 11 statuses and serialized results matched, and all 99 ordered events matched after normalizing only the two APIs' field names and per-transaction event-index origin. Six receipts matched all five cost dimensions. Five were honestly red only in runtime: tx 6687ed7f... +1,188; tx 785677c8... +16,784; and three blocksurvey proof-of-submission calls 9f556d17..., df878d9d..., 498011a3... each +32. Read/write counts and lengths all match. This is now a compiler metering blocker, not an observer-payload blocker; the task remains partial.

## 2026-08-12T13:42:24Z

2026-08-12: The retained current-replay receipts isolated the three BlockSurvey proof calls at exactly +32 runtime with all other cost dimensions, results and events matching the network. The faithful Clarity-3-at-Epoch40 reduction was +64 because it exercised two absent tuple-valued map lookups. Commit 8377f826 fixes the general cause: `get` on an optional tuple now charges TupleGet only for `some`, matching the pinned interpreter's `none` passthrough. The recorded-semantics/current-cost harness proves the real deployment boundary rather than silently upgrading Clarity semantics. After the fix, the absent/present tuple table and faithful BlockSurvey path are exact, the complete cost crosscheck is 41/41, tuple tests are 11/11, and strict clar2wasm all-target Clippy is green. This closes the causal compiler defect, not the live-evidence item: task 052 remains open until the corrected release artifact reproduces the network receipt in an isolated replay.

## 2026-08-12T16:24:15Z

2026-08-12: Closed the current-mainnet receipt cost blocker at block 8,695,878. The remaining tx 785677c8... +1,934 decomposed exactly into six bound-principal stx-get-balance copies (6 x 297) plus four slice? calls at a buffer's end (4 x 38). The pinned interpreter reads stx-get-balance's owner by reference; clar2wasm now does too. slice? had overwritten the left-bound failure with the right-bound check, returning some(empty) for (slice? seq len len); it now preserves both failures. Both focused regressions were RED before and GREEN after. Green gates: exact two-receipt isolated reflink replay (tx 6687ed7f... compiled/interpreter/network 1,234,716; tx 785677c8... all 33,861,502), STX 16/16, sequences 115/115, cost crosschecks 43/43, strict all-target clar2wasm Clippy, taskmd validation 114/114, and unchanged immutable-source DB stamps. Task remains partial only for the separately recorded non-empty reward-set payload evidence.

## 2026-08-12T16:27:03Z

2026-08-12: Closed the final reward-set evidence item. The current event_observer::new_block_payloads_match_the_ones_stacks_core_published gate replayed all 340 captured blocks with the current code, rebuilt every new_block payload from nano's AppliedBlock data, and matched stacks-core's independent observer payloads field-for-field. Its non-vacuity guards require receipts, Clarity events, matured rewards and derived reward sets; the capture contains four non-empty reward-set payloads, each with three signers. Gate: 1/1 passed in 1.32s under the private target and 6 GiB systemd cap (311.6 MiB peak, zero swap). Together with the current-mainnet receipt/cost/event/provenance evidence recorded above, this completes the last checklist item.

## 2026-08-12T17:10:44Z

Reopened 2026-08-12 after the current isolated mainnet replay exposed GET /v2/pox returning HTTP 503 while /v2/info was healthy at executed height 8,705,679. Long catch-up deliberately published only peer height; RpcState::publish_executed incorrectly recovered PoX constants only from the absent full followed view. The source fix now makes the runtime's PoXInfo an explicit, non-optional part of each executed snapshot. Closure requires focused tests, strict Clippy, and a rebuilt isolated live catch-up returning 200.

## 2026-08-12T17:17:27Z

Source gate 2026-08-12: release-profile cargo check for nano-rpc+nano-node all targets passed; exact nano-rpc tests pox_info_reads_the_exact_local_values_and_serves_the_stock_shape, pox_info_never_turns_a_bad_local_read_into_zero, and no_route_serves_a_block_the_node_has_not_executed each ran 1/1 green; strict Clippy over both crates passed. Gated under a 6 GiB systemd cap with two jobs and the build lock (1.94 GiB peak, zero swap). Live rebuilt 200 proof remains open.

## 2026-08-12T17:49:31Z

2026-08-12: The first rebuilt live restart retained PoX constants but /v2/pox still returned 503. Read-only inspection proved the sealed tip header is complete at burn 961142. The earlier dual-engine ArithmeticUnderflow clue was partly artificial because xtask call-both opened its diagnostic block at burn height zero. The live cause is cycle ownership: cycle 140 remains PoX-4 while PoX-5's first reward cycle is 141, so calling pox-5.get-pox-info before its own first burn height intentionally underflows. The route now selects the boot contract independently for each cycle: current get-pox-info and cycle 140 total use pox-4; cycle 141 total uses pox-5; contract_id names the current contract. Exact boundary-shape and fail-closed nano-rpc tests passed 1/1, release all-target nano-rpc+nano-node check passed, and strict release Clippy passed under the 5 GiB systemd cap (zero swap). A rebuilt isolated live HTTP 200 remains required before closure.

## 2026-08-12T18:49:22Z

2026-08-12: The cycle-aware 0a304f72 artifact was restarted twice against the isolated replay state. With outbound traffic denied only in that transient unit so the RPC could acquire the executor lock, GET /v2/info was healthy at Stacks 8,706,877 / burn 961,154 but GET /v2/pox returned 503: pox-4.get-pox-info ArithmeticUnderflow. Direct recorded-epoch calls proved pox-4 burn-height-to-reward-cycle(u961154)=u140 and reward-cycle-to-burn-height(u140)=u960050 in both engines, so stored PoX state was sound. The restart-specific cause was Epoch-4 burn-block-height: a reopened VM has a sealed MARF tip but its in-memory Bitcoin context is height zero until another block begins. ReadOnlyCall now carries the executed snapshot's locally derived Bitcoin context; Vm installs it before the non-committing call; both /v2/pox and generic /v2/contracts/call-read use it. Exact VM regression returned u321 after installing height 321; exact PoX and generic RPC regressions passed 1/1; nano-vm+nano-rpc+nano-node all-target check and strict all-target Clippy passed under the 5 GiB cgroup (3.8 GiB peak, zero swap). A rebuilt live HTTP 200 remains required before re-closing the task.

## 2026-08-12T18:55:46Z

2026-08-12: Live closure on immutable release artifact fd4f5dd2, SHA-256 439d5d65d5ce844673828c29c95a2f2ca615cd42385ab9886f86fccd47fa25cc. The node reopened the isolated replay state at exact sealed Stacks height 8,706,877. Outbound networking was denied only for its transient systemd unit after RPC bind so the follower released the shared executor; protected nodes were untouched. GET /v2/info returned HTTP 200 at burn 961154 / Stacks 8706877. GET /v2/pox returned HTTP 200 in 0.320s: current contract SP000000000000000000002Q6VF78.pox-4, reward cycle 140, current stacked 446974093990056, next cycle 141 stacked 50078409414457, PoX-5 first cycle 141, minimum 92808050173 and liquid supply 1856161003471429. The node stopped normally (exit 0, 'every sealed block is already on disk'); RPC/metrics ports closed; MARF, Clarity and archive inode/size/mtime stamps were unchanged. This satisfies the only reason task 052 was reopened.
