## 2026-07-27T18:05:23Z

Started bit-exact trie core: MARF values, leaves, Node4 hashing, fixed Node256 root, compressed insertion, and lookups now have in-process reference coverage.

## 2026-07-27T18:12:45Z

Implemented in-memory copy-on-write MARF versions. Unchanged child pointers hash their ancestor block IDs and have a stacks-core differential oracle.

## 2026-07-27T18:15:11Z

Added the reserved MARF height and block mapping leaves at every version boundary, matching stacks-core insertion order.

## 2026-07-27T18:24:25Z

Implemented raw stacks-core SQLite/blob checkpoint graph import, including compressed pointer arrays and trie patches. The captured checkpoint reconstructs to its published root. Its PCS squash tables are empty, so PCS-squashed input still needs a real fixture.

## 2026-07-27T19:16:00Z

Added manifest-driven PCS import: import_pcs loads the standard marf-squash PCS_manifest.toml, resolves the Clarity MARF path, and validates its published archival root. Investigated live exporter mismatch: Hacknet e04 database predates the newer marf-squash canonical-tip schema.

## 2026-07-27T19:17:03Z

Added an end-to-end conformance test for the PCS directory layout and manifest-root import API using the captured checkpoint graph.

## 2026-07-27T20:38:19Z

Added direct Stacks-Core MARF lockstep coverage for 10,000 deterministic random fork/COW writes and external-blob checkpoint import/extension; captured PCS root remains verified. Captured-checkpoint extension stays open because the minimal fixture cannot be reopened for writes by stacks-core.

## 2026-07-27T20:41:44Z

Verified the captured PCS checkpoint opens through stacks-core with external blobs and its first extension has the same root after nano import.
