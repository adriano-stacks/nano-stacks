## 2026-08-10T13:24:05Z

Pristine run /home/aldur/mainnet-pristine-20260810 stopped at executed 8,665,601 while staging grew to 67,420 blocks. Read-only staging inspection proved one coherent 66,714-block branch from 8,666,355 to 8,733,068, leaving a 753-block parent gap. Forward inventory scheduling exhausted the peer pool before the backward descent every round. Commit c06d9522 closes an existing staged gap before fetching farther ahead. Its regression was RED before the fix with fetched=10/executed=0/scheduled=1/rate_limited=true, then GREEN; inventory_schedule 4/4, catch_up_rounds 8/8, follow_path 6/6 and strict nano-node+nano-conformance Clippy passed. Task remains open until a new pristine replay proves checkpoint-to-tip execution.

## 2026-08-10T14:17:40Z

Storage replay optimization validated without changing task scope. Embedded SQLite now raises its mmap ceiling to 1 TiB; durable MARF and Clarity connections request a 64 GiB read map, and newly created stores use 16 KiB pages. On fresh reflink copies, the same 99 captured mainnet blocks (8,665,602–8,665,700) fell from 20.59–23.50 s before mmap to 15.46–18.74 s with mmap; a defragmented 4 KiB control was 17.03 s, while two full 16 KiB runs were 12.40 s and 14.26 s. At equal compaction, MARF size was 15,586,140,160 bytes at 4 KiB versus 14,249,852,928 at 16 KiB. Focused tests assert the effective 16 KiB page and 64 GiB map for both stores. nano-marf 12/12, nano-vm 38/38 (1 search ignored), MARF lockstep 3/3, checkpoint filter 11/11, kill-boundary filter 3/3, and strict nano-marf+nano-vm Clippy are green. Task remains open: this is throughput evidence, not the required pristine checkpoint-to-tip replay.
