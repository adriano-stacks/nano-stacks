---
id: "012"
title: "M11: implement HTTP synchronization and RPC"
status: in-progress
priority: high
effort: large
dependencies: ["011"]
tags: ["m11", "sync", "rpc"]
created_at: 2026-07-27
---

# M11: implement HTTP synchronization and RPC

## Objective

Follow a peer through validated HTTP tenure streams and expose the data needed
to execute and serve the node's current chain view.

## Tasks

- [x] Fetch and validate `/v2/info`, `/v2/pox`, `/v3/blocks`, and tenure streams.
- [x] Reject tenure forks and invalid block links before advancing the follower.
- [x] Exercise the follower against a local Hacknet instance.
- [ ] Retain and execute a continuous multi-cycle chain view.
- [ ] Implement the public HTTP RPC and event surface.

## Acceptance Criteria

- The follower tracks a Hacknet tip and validates each downloaded tenure.
- Live checks cover PoX data, tip-block decoding, and tenure linkage.
- A live run must span two reward cycles, including prepare phase and rollover.
